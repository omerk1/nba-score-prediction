# EXPERIMENTS.md

Decision log and research agenda for the ablation-gated feature workflow (CLAUDE.md's "Project Rules (ML experimentation)" section). Numbers → `outputs/experiments_v2.csv` / `results/sessions/<session_id>.csv`. Interpretation → here, referenced by run_name / session_id.

## 1. Ground truth

**Composite score** (minimize): `diff_mae/naive_diff_mae + 0.5 * total_mae/naive_total_mae`, both terms normalized against that same split's own freshly-recomputed naive rolling-baseline. The naive baseline's own score is **always exactly 1.5** for any fold — a mathematical identity of the formula (evaluate it on the naive predictor against itself: both ratios are 1, so `1 + 0.5*1 = 1.5`), not something that needs a separate run to establish. Every score below is judged against that fixed 1.5 floor.

**CV protocol**: 5 expanding-window folds, oldest → newest, `configs/config.yaml`'s `cv.folds`, mechanically validated by `validate_fold_definitions` (fold ordering, no overlap, no fold's val/test predating an earlier fold's own training window). Fold5 = today's `--protocol single_split` boundaries exactly.

**Reproducibility, verified at full precision**: two independent full 5-fold CV runs of the (then-)champion config, diffed at full float64 precision (raw metric dicts and actual val/test predictions, not the 4dp values in logged CSV rows) — byte-identical on every fold, every metric, every prediction. `std = 0.0` exactly. `target_formulation=diff_total`'s own determinism was separately re-verified (two independent fold5 runs, full precision, byte-identical) before adoption — see the `target_formulation_diff_total` decision-log entry.

**Champion baseline** (`target_lambda_weight_0.75`, `outputs/experiments_v2.csv`): `style_matchup.raw_features_enabled=true`, `preferred_opponent_delta_enabled=true`, `style_matchup.enabled=false`, `model.target_formulation=diff_total`, `model.target_lambda_weight=0.75`.

| fold | val_score | test_score |
|---|---:|---:|
| 1 | 1.4368 | 1.3948 |
| 2 | 1.3898 | 1.3852 |
| 3 | 1.3719 | 1.3713 |
| 4 | 1.3527 | 1.3713 |
| 5 | 1.3592 | 1.3299 |
| **mean** | **1.3821** | **1.3705** |

**Fold-1 gap, called out explicitly**: fold1's val_score (1.4368) is ~4–6% worse than every other fold (1.35–1.39 range) — the smallest training window (2018-10-16 → 2020-08-14) is the weakest fold by a clear margin, not noise. Section 2's fold-1 breakout and section 3's diagnostics/experiments (run under the prior `home_away` champion) are largely about understanding and addressing this gap; it persists under the current champion too, just slightly narrowed.

**Superseded reference points** (all kept in `experiments_v2.csv` for provenance, never overwritten, per CLAUDE.md):
- `champion_cv_baseline` (val_score_mean 1.3850) → `champion_cv_baseline_post_injury_fix` (1.3851): PR #44 fixed a bug in `injury_layer.py`'s multi-archetype injury-delta accumulation. Effect was negligible (fold1 exactly unchanged, folds 2–5 shifted −0.0019 to +0.0028, no consistent direction) — see PR #44/#45.
- `champion_cv_baseline_post_injury_fix` (1.3851) → `champion_cv_baseline_diff_total` (1.3838): the target-reformulation experiment (section 3.3) — fitting `MultiRMSE` on `[POINT_DIFF, TOTAL_POINTS]` instead of `[PTS_home, PTS_away]` beat the old formulation on val_score in **all 5 folds individually**, the first candidate in this project's CV-protocol history to cleanly clear the per-fold guardrail. `configs/config.yaml`'s `model.target_formulation` is now `diff_total`.
- `champion_cv_baseline_diff_total` (1.3838) → `target_lambda_weight_0.75` (1.3821): the `target_lambda_weight` sweep (section 3.3 follow-up) — 0.5 had only ever been inherited from `compute_composite_score`'s own diff/total weighting, never independently tuned for the training loss; a cheap 3-fold screen found 0.75 best, and full 5-fold CV confirmed it beats 0.5 on val_score in 4 of 5 folds (only fold2 regresses). `configs/config.yaml`'s `model.target_lambda_weight` is now `0.75`. No dedicated `champion_cv_baseline_*` row this time — the experiment's own row already has byte-identical values, so a second row would be pure duplication; `EXPERIMENTS.md` (here) is the pointer, not the CSV row name.

## 2. Family inventory findings

5-fold mean, from the family-importance inventory (`scripts/run_family_importance.py`, session `20260805_1611_family-inventory`). Ranked by permutation importance (destroy the family, how much does composite score worsen) — CatBoost's own split-importance is shown alongside since the two disagree in informative ways.

| Rank | Family | Perm Δ (mean) | CatBoost share | Fold1 : later ratio |
|---|---|---:|---:|---:|
| 1 | `style_fingerprint_features` | 0.0342 | 27.4% | 1.13 |
| 2 | `elo_features` | 0.0303 | 5.8% | 0.74 |
| 3 | `style_features` | 0.0174 | 20.1% | 1.06 |
| 4 | `matchup_features` | 0.0115 | 8.9% | 0.94 |
| 5 | `rolling_features` | 0.0077 | 12.0% | 1.21 |
| 6 | `opponent_quality_features` | 0.0022 | 9.8% | 0.87 |
| 7 | `injury_features` | 0.0017 | 1.4% | **0.00** |
| 8 | `travel_features` | 0.0014 | 3.7% | 1.19 |
| 9 | `h2h_features` | 0.0011 | 3.1% | 0.92 |
| 10 | `home_advantage_features` | 0.0007 | 4.4% | 0.89 |
| 11 | `rest_features` | 0.0007 | 1.4% | 1.06 |
| 12 | `basic_features` | 0.0004 | 1.75% | 0.21 |
| 13 | `season_motivation_features` | **−0.0003** | 0.27% | 0.56 |

**Signal is concentrated in the top 6**: they account for ~95% of total permutation delta (0.103 of 0.109); the bottom 7 combined are ~5%, and one of them is negative.

**Elo is the efficiency standout**: 6th by CatBoost share but 2nd by permutation cost. CatBoost under-splits on it — likely correlated with rolling/style as another "team strength" proxy, so a split-selection algorithm doesn't need it when substitutes are available — but the model can't fully recover once it's actually removed. The mirror case is `opponent_quality_features` (4th by share, 6th by permutation): real CatBoost usage, almost no permutation cost — "used but not predictive," likely redundant with rolling/style rather than adding anything of its own.

**`style_fingerprint_features` is the earning-but-verify dominator**: 27.4% share, also the single highest permutation cost — not just a CatBoost artifact, genuinely earning its place. Flagged for a leakage re-audit anyway (section 3.1) purely because of magnitude: its point-in-time construction was verified once, in isolation, before this session's CV-fold-boundary work existed — that verification hasn't been re-run against fold boundaries specifically.

**Fold-1 zeros/near-zeros, not all the same story**: `injury_features` is a literal, hard zero on fold1 — I believe this is a real data-coverage gap (injury data likely doesn't exist that far back), not a modeling weakness; section 3.1 makes confirming that (and deciding the fix) a gate before treating it as a feature-quality problem. `basic_features` (`season_progress`, ratio 0.21) and `season_motivation_features` (ratio 0.56) are both substantially reduced on fold1 too, but neither is a hard zero — plausible genuine data-hunger (within-season position needs more of the season observed; motivation signals need enough season-progression variance to matter).

**`injury_features`' 0.0017 mean permutation delta (rank 7) is understated, not weak** — it's a 5-fold average that blends a forced 0.0 on fold1 (no data at all, not a real predictive-value reading) with whatever the family actually contributes on folds 2–5. The true value in folds where the data exists is higher than 0.0017 suggests. Do not read this number as a prune signal, and don't group it with the genuinely weak tail (`travel`/`h2h`/`home_advantage`/`rest`) on the strength of the aggregate alone — section 3.2's E4 gets the real per-fold picture instead of relying on this diluted mean.

**`season_motivation_features` is the clear prune candidate**: the only family with a negative permutation delta — removing it very slightly helps on average, despite having passed its own single-metric ablation test at adoption time (`preferred_opponent_delta_treatment`, single_split). Not a contradiction of that earlier result, just a different, more holistic lens now that the full feature set + CV are both in play.

## 3. Research agenda

Every item below: hypothesis, config change, protocol, expected effect, effort, risk. Screening protocol note — "cheap 3-fold" (folds 3–5, per CLAUDE.md) only applies when fold1 isn't the object of study; fold1-specific questions need fold1 in the run, so cheap-3-fold does not apply to those.

### 3.1 Diagnostics (must-run gates — before anything downstream treats their targets as a modeling problem)

**D1. Injury data-coverage check.**
- Hypothesis: `injury_features.sqlite` doesn't cover fold1's train/val window at all (already know it starts 2021-10-19; fold1's val ends 2021-05-16) — the fold1 zero is a coverage gap, not a signal-quality problem.
- Action: query `injury_features.sqlite` per season for row counts / completeness, not just the min date (confirm there isn't a partial-coverage gap somewhere else in the covered range too).
- Decision this gates: confirms coverage before E4 (section 3.2) empirically compares the actual handling strategies rather than picking one by inspection. Not a training run itself — output is a coverage confirmation feeding E4.
- Effort: low (one query + a written decision). Risk: none. Screening: n/a.
- **Confirmed (session `rs_20260808_1`)**: direct per-fold query against `injury_features.sqlite` (`scorer='formula'`) — `fold1`: 0 dates with data in both train (≤2020-08-14) and val (2020-12-22→2021-05-16) windows, its test window (2021-10-19→2022-04-10) has full coverage (165/165 dates). `fold2`: train (≤2021-05-16) is *also* 0 coverage — the training window itself predates the data, not just val. `fold3` onward: full coverage in every window (train/val/test all ≥160 dates). No coverage exists anywhere before 2021-10-19, and coverage is dense (155–165 distinct dates/season, effectively every game day) once it starts — no partial-coverage gaps in the covered range. Hypothesis confirmed exactly: this is a hard data floor, not a signal-quality problem. Decision: E4 variant (c) (folds-start-at-2021) should be understood precisely as "exclude fold1 and fold2's *training* window," not just "exclude fold1" — fold2's train set has zero injury rows too, even though fold2's val/test don't.

**D2. `style_fingerprint_features` leakage re-audit.**
- Hypothesis: the family's dominance (27.4% share, highest permutation cost) is genuine, not a fold-boundary leak that the original (pre-CV-harness) leakage verification wouldn't have caught.
- Action: reuse this session's empirical audit technique (compare truncated-history vs. full-history fingerprint computation for a sample of games right at each fold's `test_start_date`) — confirm the first test-period game for a team only reflects fingerprint history strictly before that fold's own boundary.
- Expected effect: pass (no leak) is the expected/likely outcome, given the underlying `.shift(1)` construction; if it fails, this supersedes every other item in this section.
- Effort: low-medium (a script, no retraining). Risk: none (read-only). Screening: n/a.
- **Result (session `rs_20260808_1`, `scripts/audit_fingerprint_leakage.py`): PASS, no leakage.** For each of the 5 folds, sampled the first 50 test-period games (500 team-game instances, 3000 metric comparisons total) and recomputed each one from scratch using only `box_score_stats`/`player_injuries` rows dated strictly/at-most that game's own date — every value production returns is exactly reproducible (0 unexplained mismatches out of 3000, `abs diff < 1e-6` everywhere) from a from-scratch, same-date-bounded recomputation. `.shift(1)` + backward-only `merge_asof` is confirmed correct at real fold boundaries, not just in isolation.
- **Bug found along the way (not leakage, flagged separately): `injury_layer.py`'s multi-archetype delta accumulation.** First audit pass (layer=1-only truncated recompute vs. production, all 6 metrics) showed 608/3000 "mismatches" — a false start: 5 of 6 metrics are actually served from layer=2 (injury-adjusted) in production, and the gap was the legitimate injury delta, not leakage. Adding the correct same-date injury delta closed all but 64/3000. Root cause, confirmed by exactly replicating `injury_layer.py`'s own loop: `build_injury_adjusted_fingerprints` computes each archetype's delta as `fp2.at[idx, metric] = row[metric] + delta * severity_mult` — reading from the *original* layer=1 `row[metric]` every iteration instead of the running `fp2.at[idx, metric]`. When 2+ different-archetype players are out for the same team on the same date and their `injury_impact` dicts share a metric (common — `combo`'s dict touches 5 of 6 metrics), only the last-iterated archetype's delta survives; earlier ones are silently overwritten instead of summed. Replicating this exact overwrite-not-accumulate behavior reproduces production bit-for-bit (confirms the mechanism, not a guess). Affects 64/3000 = 2.1% of sampled comparisons — real, but not leakage (no information from after the game's own date is involved), and not something D2 was chartered to fix.
- **Logged as "proposed, pending review"** per the execution-scope guardrail: this is a correctness fix to existing feature *values* (not a new feature, not a feature-set structure change), which is ambiguous under "flip config flags / tune parameters" — treated as out-of-scope-for-execution per the guardrail's own tie-breaker ("when unsure, treat as proposed"). Suggested fix: change the accumulation line to add onto the running `fp2.at[idx, metric]` instead of re-deriving from `row[metric]` each iteration. Until fixed, `style_fingerprint_features`' injury-adjusted values are a systematic (bug-consistent, not random) under-count of the true multi-injury adjustment on the ~2% of team-games where multiple different-archetype players are simultaneously out — worth noting as a caveat on section 2's `style_fingerprint_features` dominator finding (27.4% share), though far too small a fraction of games to explain that magnitude on its own.
- **Next-session candidate (not yet scheduled):** fix the accumulation line, then re-run `champion_cv_baseline` fresh under full 5-fold CV and log it as its own new dated `experiments_v2.csv` row (not a retroactive edit to the existing champion row — the baseline itself moves, however slightly, so it needs its own validation run per the ablation-gated workflow). Won't change any of this session's E1/E2/E4 conclusions (the bug is orthogonal to every axis those experiments varied), but gives a more accurate absolute champion score and a truer read on `style_fingerprint_features`' real importance. Cheap (one CV run, no new script needed — reuses the existing champion-run pattern).

### 3.2 High-value experiments (pending D1/D2 passing)

**E1. Expand Elo.**
- Hypothesis: current `k_factor=11.02`/`season_regression=0.522` were tuned via `scripts/tune_elo.py` under the old single_split protocol, predating the CV harness and never re-validated per-fold. Given Elo's fold1 weakness (ratio 0.74) and its efficiency (high permutation cost relative to modest CatBoost share), retuning — or specifically testing whether a different `season_regression` narrows the fold1 gap — is worth it.
- Protocol: **full 5-fold, not cheap-3-fold** (fold1 is the whole point).
- Expected effect: fold1 val_score improves without materially moving folds 2–5 (guardrail: must hold on 2–5, not just fix fold1).
- Effort: medium (small grid/Optuna sweep via existing `tune_elo.py` infra, re-run under CV instead of single_split). Risk: low (isolated to `elo_features` config).
- **Result (session `rs_20260808_1`, `scripts/tune_elo_cv.py`): no promotion.** `tune_elo.py` couldn't be reused as-is (single-split only, and its fixed CatBoost params — depth 3, subsample 0.92 — differ from `run_split`'s actual depth 6/subsample 0.8, so tuning against it wouldn't transfer); wrote a new script that grid-searches `season_regression` ∈ {0.30, 0.40, 0.522, 0.65, 0.80} through `run_split` unmodified, full 5-fold per candidate (`k_factor` held at 11.02). `season_regression=0.522` (current default) reproduced `champion_cv_baseline` exactly (val_mean=1.3850, per-fold identical to 4dp) — a nice byproduct confirmation of the determinism finding from section 1. Best candidate: `season_regression=0.65`, val_mean=1.3841 (Δ−0.0009 vs. champion). Per-fold delta (0.65 − champion): fold1 −0.0059, fold2 +0.0025, fold3 +0.0018, fold4 −0.0059, fold5 +0.0030 — improves fold1 (the target) and fold4, but regresses folds 2/3/5. Guardrail requires holding on folds 2–5, not just fold1; 3 of those 4 folds got worse, so this does not qualify despite the marginally better mean.
- Conclusion: **not promoted.** No tested `season_regression` value improves on the champion in a way that survives the fold-2–5 guardrail; the whole 1-D grid is essentially flat (val_mean range 1.3841–1.3852, well under a 0.001 spread — deterministic, not noise, but not a meaningful lever either). Decided **not** to extend to the `k_factor` cross-grid (`--grid both`, 15 candidates): with the single axis most directly tied to the fold1-cold-start hypothesis already showing no clean win, and mixed (not uniformly fold1-favoring) per-fold behavior, a 3x-larger grid looked unlikely to change the verdict — reallocating remaining session time to E2/E4, which have sharper, more specific hypotheses (E4 especially, given D1 also found fold2's *training* window has zero injury coverage, another concrete, already-diagnosed lever on the same cold-start problem this experiment was chasing).
- Next: none planned on this axis for now. `k_factor`/`home_advantage` retuning remains a candidate for a future, separately-scoped session if the E4/E2 results suggest revisiting Elo specifically.

**E2. Prune the dead tail.**
- Hypothesis: `season_motivation.preferred_opponent_delta_enabled: false` performs flat-to-marginally-better than `true` under full CV (matches its negative permutation delta).
- Protocol: cheap 3-fold screen (folds 3–5) first; full 5-fold only if the cheap screen looks promising enough to consider promoting.
- Expected effect: val_score_mean roughly flat or very slightly improved.
- Effort: very low (single flag flip, `scripts/run_fingerprint_ablation.py`'s pattern reused for this flag instead). Risk: very low.
- Watch list, not bundled into E2's own test (avoid conflating simultaneous changes): `rest_features`, `home_advantage_features` — both near-zero permutation cost though not negative. Worth their own single-flag checks later, not this run.
- **Result (session `rs_20260808_1`, `scripts/run_season_motivation_ablation.py`): cheap-screen only, not escalated.** Folds 3–5: on mean val=1.3656/test=1.3605, off mean val=1.3650/test=1.3519. Per-fold delta (on−off): fold3 −0.0019, fold4 **+0.0087**, fold5 −0.0051 — 2 of 3 folds favor off (matches the hypothesis direction), fold4 favors keeping it on fairly clearly. Overall val_mean improvement from turning it off is +0.0006 — matches the expected "roughly flat" outcome exactly, not a real lever either way.
- Conclusion: **not escalated to full 5-fold this session.** Effect is tiny and directionally mixed (not the "looks promising enough to consider promoting" bar the protocol set for escalation) — reallocated the remaining time to E4, which has a sharper, D1-gated hypothesis. `preferred_opponent_delta_enabled` stays `true` (unchanged).
- Next: candidate for a future cheap addition (very low effort) if a session has idle budget, but not a priority — the tiny magnitude here doesn't justify a full-5-fold run on its own.

**E3. Refine top families — collinearity check.**
- Hypothesis: `style_fingerprint_features` and `style_features` (both broad "team style/quality" proxies) and/or `opponent_quality_features` (the "used but not predictive" family from section 2) have real overlap — consolidating could simplify the model without losing signal, or confirm `opponent_quality_features` is safe to prune alongside `season_motivation_features`.
- Action: correlation/VIF analysis between the three families' engineered columns — diagnostic only, no retraining required first.
- Effort: medium (a script). Risk: none for the diagnostic; any resulting drop-a-family experiment inherits E2's protocol.
- **Result (session `rs_20260808_1`, `scripts/family_correlation_vif.py`, fold5 train features, 7,059 rows, 48 columns across the 3 families):** cross-family correlation is modest, not the "real overlap" the hypothesis expected — mean |r|: `style_fingerprint_features` vs. `style_features` 0.163, `style_fingerprint_features` vs. `opponent_quality_features` 0.162, `style_features` vs. `opponent_quality_features` 0.209 (all well under a redundancy-level threshold). Highest single cross-family pair: `away_style_pace_score` vs. `away_team_def_eff_L20`, r=0.753 — a real but isolated overlap between two individual columns, not evidence of broad family-level redundancy. Mean VIF by family: `style_fingerprint_features` **1.31** (essentially no collinearity issue anywhere), `opponent_quality_features` 5.72, `style_features` 6.25. Only 2/48 columns exceed the conventional VIF=10 flag (`home/away_team_off_eff_L10`, 11.9/11.3) — and those are *within-family* (both `style_features`), not cross-family.
- Conclusion (**proposed, pending review** — no consolidation executed): the original hypothesis doesn't hold at the family level — `style_fingerprint_features` (the 27.4%-share dominator) is essentially collinearity-free and stays distinct from the other two, arguing *against* touching it. What the VIF numbers actually show is **within-family** redundancy in `style_features` and `opponent_quality_features` specifically — their own multiple rolling-window variants (L5/L10/L20 of the same underlying efficiency/quality stat) correlate heavily with each other. A more targeted, better-supported proposal than "merge families": test trimming redundant rolling windows *within* `style_features`/`opponent_quality_features` (e.g., L5+L20 only, dropping L10) rather than any cross-family consolidation. This is a refinement of the original hypothesis, not a confirmation of it — logged for review, nothing executed.
- Next: a scoped rolling-window-trim experiment for `style_features`/`opponent_quality_features` is a better-motivated future item than the original family-merge idea; not run this session.

**E4. Injury missing-data handling** (depends on D1; explicitly not part of E2's prune group — `injury_features`' aggregate score is understated, not weak, see section 2).
- Hypothesis: how missing injury data is represented matters more than whether the feature itself is weak. Three variants, compared directly:
  1. Native CatBoost NaN handling (status quo — missing rows pass through untouched).
  2. Explicit availability-indicator feature (`home/away_team_injury_data_available`) alongside the existing columns.
  3. Folds-start-at-2021 — restrict the comparison to the date range where coverage actually exists (drops/truncates fold1, per D1's coverage findings).
- Protocol: each variant screened only on the folds where it's actually comparable to the others (variants 1–2 run across all 5 folds; variant 3 is inherently restricted to the covered range, so its comparison is scoped to whichever folds overlap). Not a cheap-3-fold screen — this is fold1-relevant by construction.
- Expected effect: may lift fold1 specifically; watch per-fold, not just the mean (guardrail applies — no promotion on a fold1-only improvement).
- Effort: medium (indicator feature is a small `feature_builder.py` addition; the fold-restriction variant reuses existing fold-filtering, no harness changes). Risk: low.
- **Reframed before running** (see EXPERIMENTS.md's flagged findings from planning): "variant 1: native NaN, status quo" was inaccurate — the code actually zero-fills; and "variant 2: availability indicator" already ships (`has_injury_data`). Real comparison run: `injury_features.missing_value_strategy: zero_fill` (true status quo) vs. `native_nan` (new — a config-gated code change removing the `fillna(0)` imputation), full 5-fold each. Variant 3 (folds-start-at-2021) computed as a folds3-5-only slice of these same two runs, not a third training pass (D1 confirmed folds 1–2 have zero *training*-time coverage, so folds 3–5 are where the strategy can actually matter).
- **Result (session `rs_20260808_1`, `scripts/run_injury_missingdata_comparison.py`):** full-5-fold mean val: zero_fill=1.3850, native_nan=1.3849 — nearly identical. Per-fold delta (native_nan − zero_fill): fold1 −0.0051, fold2 −0.0001, fold3 **+0.0079**, fold4 −0.0060, fold5 **+0.0027**. **folds3-5-only mean val: zero_fill=1.3656, native_nan=1.3672 — native_nan is *worse* specifically on the folds where the feature has real training-time signal.** The apparent full-5-fold parity is fold1's improvement (a fold with zero training-time injury data either way) offsetting a real regression on folds 3–5. (Test scores logged only, not decision-driving per CLAUDE.md's hard constraint — they actually point the opposite direction on folds 3–5, underscoring why val governs.)
- Conclusion: **not promoted; original hypothesis not supported.** This is exactly the guardrail's fold1-alone trap in a new form — the only fold(s) where `native_nan` looks good are the ones D1 already proved carry no real injury signal during training either way, and on the folds that do carry signal, it's mildly worse. `missing_value_strategy` stays `zero_fill`. Closes out E4 as a diagnostic question: representation strategy isn't the fold1-gap lever; the coverage floor itself (D1) is the real constraint, and no missing-value encoding trick recovers information that was never in the training data.
- Next: none — the config flag stays in place (disabled-from-adoption, ships at `zero_fill`) for any future revisit, but this line is closed.

### 3.3 Model-agnostic axes (broader, lower immediate priority — not driven by the family inventory directly)

- **Target reformulation — executed and ADOPTED.** See the `target_formulation_diff_total` decision-log entry below (section 4): implemented as `model.target_formulation` config flag (`ScorePredictor._to_training_targets`/`_from_training_targets`), full 5-fold CV comparison run. `diff_total` beat `home_away` on val_score in all 5 folds, mean 1.3838 vs. 1.3851 — the first candidate in this project's CV-protocol history to cleanly clear the per-fold guardrail. Re-verified after an explicit re-audit (CatBoost's `MultiRMSE` loss formula confirmed against official docs, determinism re-checked, save/load round-trip tested) before adoption. `configs/config.yaml`'s `model.target_formulation` is now `diff_total` — this is the new standing champion.
- **Hyperparameter tuning**: CatBoost's `depth`/`learning_rate`/`iterations`/`subsample`/`colsample_bylevel` are hardcoded in `cv_harness.run_split`, never tuned via config or CV.
- **Calibration**: brier-score / win-probability calibration check (isotonic or Platt scaling against the normal-CDF approximation currently used).
- **Model-class, narrowed to CatBoost-internal**: compare `MultiRMSE` vs. two single-target `RMSE` models vs. `Quantile` loss (for the calibration angle) — other GBDT libraries or NN architectures deprioritized given dataset size (~2.3k–7k training games/fold), see prior discussion.

## 4. Decision log

**Guardrail (applies to every entry below and every future one)**: per-fold deltas must be shown, not just the mean. No experiment is promoted on fold1's strength alone — an improvement must hold on folds 2–5.

**No significance floor on top of that**: the champion config was verified byte-identical (see section 1) across two independent full 5-fold CV runs — `std = 0.0` exactly, not just small. Only the champion config was directly tested, but the mechanism is config-independent (`ScorePredictor`'s fixed `random_state` governs CatBoost's own bagging RNG regardless of which features are enabled, and nothing else in the pipeline introduces unseeded randomness), so this should generalize to any config in the agenda. Since there is no measured run-to-run variance at all, an experiment's reported delta is never competing with sampling noise; the per-fold-deltas-required guardrail above is sufficient on its own. Adding an arbitrary significance multiplier here would be inventing a floor the evidence doesn't call for.

**Autonomous-session execution scope**: an unattended run may fully execute anything that only flips a config flag or tunes a parameter within the existing model/loss/metric — this covers both diagnostics (D1, D2) and the config-level experiments (E1 Expand Elo, E2 Prune the dead tail, E4 Injury missing-data handling). It may run E3's correlation/VIF analysis (diagnostic, no retraining). It must NOT execute, on its own authority, any change to model architecture, the training loss, or the composite metric itself — specifically: E3's actual family-consolidation action (dropping or merging `style_fingerprint_features`/`style_features`/`opponent_quality_features` columns, once the VIF analysis motivates it) and the 3.3 target reformulation (`[home,away]` → `[diff,total]` changes what `MultiRMSE` is fit against, i.e. changes the loss). Anything in this second category gets logged in section 4 as **"proposed, pending review"** — hypothesis, the analysis that motivates it (e.g. the VIF numbers, or the loss-alignment argument), and expected effect — but not run, pending my review.

---

**`champion_cv_baseline`** (`outputs/experiments_v2.csv`, no session_id — manual one-off)
- Hypothesis: establish the reference point for all CV-protocol comparisons going forward, using the already-adopted single_split feature set re-evaluated under the audited/leak-fixed CV harness.
- Result: val_score_mean 1.3850, test 1.3724. Per-fold val: 1.4407 / 1.3876 / 1.3804 / 1.3579 / 1.3585. Fold1 is the clear outlier (see section 1).
- Conclusion: this is the standing champion under CV. Nothing compared against it yet has beaten it.
- Next: section 3's agenda.

**`style_matchup_knn_fixed_cv`** (session `20260805_1611_family-inventory`)
- Hypothesis: `matchup_index.py`'s `FINGERPRINT_METRICS` bug (missing `offensive_rating`, 5 metrics instead of 6 — fixed in #40) materially changes the KNN-similarity feature's behavior; re-test under full CV (never tested under CV before, only single_split in July, not adopted).
- Result: mean val 1.3830, test 1.3690. Per-fold val: 1.4341 / 1.3846 / 1.3788 / 1.3521 / 1.3653. Per-fold test: 1.3862 / 1.3873 / 1.3718 / 1.3620 / 1.3379.
- Conclusion: a small measured effect vs. champion (1.3850/1.3724 → 1.3830/1.3690), not clearly distinguishable from zero — deterministic delta from a genuine 2-feature config difference (129 vs. 127 features, `style_matchup_score`/`confidence`), not stochastic noise. The underlying fix is real (99.5% of a 200-game sample had a changed similarity score) but doesn't change the adoption verdict. Not adopted; `style_matchup.enabled` stays `false`.
- Next: none planned — this line is closed pending a genuinely new signal (e.g. richer style inputs, per the design doc's Future Work), not a rerun of the same feature.

**`fingerprint_ablation_on_cheap3fold` / `fingerprint_ablation_off_cheap3fold`** (session `20260805_1611_family-inventory`)
- Hypothesis: does the adopted raw-fingerprint block (18 dims) still earn its place under the real, leak-fixed CV harness (previously only validated via the pre-this-session `walkforward.py` CV)?
- Result (folds 3–5 only, cheap screen): on mean val 1.3656/test 1.3605, off mean val 1.3725/test 1.3620. Per-fold (on / off / delta): fold3 1.3804/1.3912/−0.0108, fold4 1.3579/1.3565/+0.0014, fold5 1.3585/1.3699/−0.0114 (val); fold3 1.3720/1.3756/−0.0036, fold4 1.3787/1.3689/+0.0098, fold5 1.3308/1.3417/−0.0108 (test).
- Conclusion: 2 of 3 folds favor keeping it on, 1 (fold4) favors off — not unanimous, but directionally consistent with section 2's finding that this family has the highest permutation cost of all 13. Re-confirms the original adoption under a more rigorous, independently-built harness.
- Next: none planned — already adopted, this was re-verification, not a new proposal.

---

**Session summary — `20260805_1611_family-inventory`**
- Explored: the `FINGERPRINT_METRICS` bug fix + CV re-test of `style_matchup.enabled`; the raw-fingerprint on/off cheap-3-fold ablation; the 13-family CatBoost + permutation importance inventory across all 5 folds.
- Promoted: nothing new to `experiments_v2.csv` beyond `champion_cv_baseline` itself (already logged separately, not part of this session's own runs).
- Dropped: nothing newly disabled — `style_matchup.enabled` confirmed staying `false`, `raw_features_enabled` confirmed staying `true`.
- Produced: the family-importance inventory that drives section 3's entire agenda — this session's main output was diagnostic/evidence-gathering, not a feature change.

---

**`D1` — injury data-coverage check** (session `rs_20260808_1`)
- Hypothesis: `injury_features.sqlite` doesn't cover fold1's train/val window at all — the fold1 zero is a coverage gap, not a signal-quality problem.
- Result: confirmed exactly, plus one refinement — fold2's *training* window (≤2021-05-16) also has zero coverage, not just fold1. Fold3 onward: full, dense coverage (155–165 distinct dates/season) in every window. No coverage gaps anywhere else in the covered range.
- Conclusion: hard data floor, not a signal-quality problem. Gates E4's fold-restriction variant.
- Next: E4.

**`D2` — `style_fingerprint_features` leakage re-audit** (session `rs_20260808_1`, `scripts/audit_fingerprint_leakage.py`)
- Hypothesis: the family's dominance (27.4% share, highest permutation cost) is genuine, not a fold-boundary leak.
- Result: PASS, no leakage — 0/3000 unexplained mismatches across all 5 folds' fold-boundary samples (see section 2 for full detail). Along the way, found and confirmed (by exact replication) a real but unrelated correctness bug in `injury_layer.py`'s multi-archetype delta accumulation (overwrite instead of sum when 2+ archetypes share an affected metric on the same team/date) — affects 64/3000 = 2.1% of sampled comparisons.
- Conclusion: champion is not invalidated — no leakage found. The accumulation bug is logged as **proposed, pending review** (a correctness fix to existing feature values, not executed per the execution-scope guardrail), not blocking the rest of this session's agenda.
- Next: E1, E2, E4, E3 proceed as planned.

**`E1` — Expand Elo** (session `rs_20260808_1`, `scripts/tune_elo_cv.py`)
- Hypothesis: `season_regression=0.522` (tuned pre-CV-harness, single_split) may be narrowable to reduce fold1's cold-start gap.
- Result: full 5-fold grid over `season_regression` ∈ {0.30, 0.40, 0.522, 0.65, 0.80}, `k_factor` held at 11.02. Best: `season_regression=0.65`, val_mean=1.3841 vs. champion's 1.3850. Per-fold delta (best − champion): fold1 −0.0059, fold2 +0.0025, fold3 +0.0018, fold4 −0.0059, fold5 +0.0030.
- Conclusion: **not promoted** — improvement doesn't hold on folds 2–5 (3 of 4 non-fold1/4 folds regressed), guardrail violation despite a marginally better mean. Whole grid essentially flat (1.3841–1.3852 range). `season_regression=0.522` exactly reproduced `champion_cv_baseline`'s numbers to 4dp, a useful incidental determinism re-confirmation.
- Next: `k_factor` cross-grid deliberately not run (see section 3.2's full writeup for rationale) — time reallocated to E2/E4.

**`E2` — Prune the dead tail** (session `rs_20260808_1`, `scripts/run_season_motivation_ablation.py`)
- Hypothesis: `preferred_opponent_delta_enabled=false` performs flat-to-marginally-better than `true` (matches its negative permutation delta).
- Result: cheap 3-fold screen (folds 3–5). On mean val=1.3656/test=1.3605, off mean val=1.3650/test=1.3519. Per-fold delta (on−off): fold3 −0.0019, fold4 +0.0087, fold5 −0.0051.
- Conclusion: **not escalated to full 5-fold** — matches the "roughly flat" expected effect exactly (+0.0006 mean), but direction is mixed across folds (2/3 favor off, fold4 favors on). Doesn't clear the "promising enough to escalate" bar the protocol set. `preferred_opponent_delta_enabled` stays `true`, unchanged.
- Next: none planned this session — low-priority future candidate given the tiny magnitude either way.

**`E4` — Injury missing-data handling** (session `rs_20260808_1`, `scripts/run_injury_missingdata_comparison.py`)
- Hypothesis: representation of missing injury data (zero_fill vs. native_nan) matters more than the feature's aggregate weakness; may lift fold1 specifically.
- Result: full 5-fold, both variants. Full-5-fold mean val nearly identical (zero_fill=1.3850, native_nan=1.3849), but folds3-5-only mean val: zero_fill=1.3656 vs. native_nan=1.3672 — native_nan is worse specifically on the folds with real training-time injury coverage. Per-fold delta (nan−zf): fold1 −0.0051, fold2 −0.0001, fold3 +0.0079, fold4 −0.0060, fold5 +0.0027.
- Conclusion: **not promoted; hypothesis not supported.** native_nan's only apparent gains are on fold1/fold2, which D1 already proved have zero training-time injury data regardless of encoding — the guardrail's fold1-alone trap, recognized and not acted on. `missing_value_strategy` stays `zero_fill`. Closes the diagnostic: D1's coverage floor is the real constraint, not encoding.
- Next: none — config flag stays in place, disabled-from-adoption, for any future revisit.

**`E3` — Refine top families, collinearity check** (session `rs_20260808_1`, `scripts/family_correlation_vif.py`, diagnostic only — no consolidation executed)
- Hypothesis: `style_fingerprint_features`/`style_features`/`opponent_quality_features` have real cross-family overlap worth consolidating.
- Result: cross-family mean |r| modest throughout (0.16–0.21). Mean VIF: `style_fingerprint_features` 1.31 (no collinearity issue), `opponent_quality_features` 5.72, `style_features` 6.25. Only 2/48 columns exceed VIF=10, both within `style_features` (its own L10/L20 rolling-window variants of `off_eff`).
- Conclusion: **proposed, pending review — hypothesis not confirmed as stated.** The dominant family is collinearity-free and distinct; the real redundancy is *within* `style_features`/`opponent_quality_features` (their own multiple rolling windows), not *across* the three families. A rolling-window trim is the better-motivated next step than a family merge — logged, not executed.
- Next: none this session — a scoped follow-up experiment, not a priority.

---

**Session summary — `rs_20260808_1`**
- Explored: full section 3 agenda in order — D1 (injury coverage), D2 (fingerprint leakage gate), E1 (Elo grid), E2 (season_motivation prune screen), E4 (injury missing-data handling), E3 (collinearity/VIF diagnostic).
- Gates: D1 confirmed the coverage floor exactly (plus a refinement — fold2's train window is also affected, not just fold1). D2 passed clean, no leakage — champion not invalidated.
- Found along the way (not part of the planned agenda): a real correctness bug in `injury_layer.py`'s multi-archetype injury-delta accumulation (overwrite instead of sum, ~2% of team-games affected). Not leakage, doesn't invalidate anything this session concluded (orthogonal to every experiment's own varied axis). Logged as **proposed, pending review** — not fixed.
- Promoted to `outputs/experiments_v2.csv`: two rows, for leaderboard completeness only (CLAUDE.md's "any row that beats the current champion" rule) — `elo_sr0.65_k11.02` (val_score_mean=1.3841) and `injury_missingdata_native_nan_full5fold` (val_score_mean=1.3849). **Neither is adopted or the new champion** — both explicitly fail the per-fold guardrail (fold1-driven, not holding across folds 2–5), exactly the pattern the guardrail exists to catch. `champion_cv_baseline` remains the standing champion; `configs/config.yaml`'s adopted defaults are unchanged from `main`.
- Dropped/not promoted: E1 (Elo retune), E2 (season_motivation prune, not even escalated to full CV), E4 (injury missing-data handling) — all negative or inconclusive results, all informative.
- Proposed, pending review (not executed, per the execution-scope guardrail): the `injury_layer.py` accumulation-bug fix (D2), and E3's rolling-window-trim follow-up (a more precise, better-motivated proposal than the original family-merge hypothesis it replaces).
- Net effect on the champion: none — no promotion this session. The main value was diagnostic: a clean leakage bill of health for the dominant feature family (with one real bug found and flagged, not fixed), three ruled-out directions (Elo season_regression, season_motivation pruning, injury missing-data encoding) with clear per-fold evidence for each, and a corrected, more actionable version of the collinearity hypothesis for a future session.
- Config state: `configs/config.yaml` on this branch differs from `main` only by the new (disabled-from-adoption) `injury_features.missing_value_strategy: zero_fill` field added for E4 — no adopted defaults changed.

---

**`target_formulation_home_away` / `target_formulation_diff_total`** (manual one-off, `scripts/run_target_formulation_experiment.py`)
- Hypothesis (section 3.3): fitting `MultiRMSE` on `[POINT_DIFF, TOTAL_POINTS]` instead of `[PTS_home, PTS_away]` should improve the composite score by aligning the training loss with what it actually rewards (diff-dominant, total at half weight).
- Implementation: `model.target_formulation` config flag (`home_away` default / `diff_total`), entirely internal to `ScorePredictor` — `_to_training_targets`/`_from_training_targets` convert in/out of fit-space, but `predict()`/`evaluate()`'s public contract always stays `[home, away]`. `diff_total` mode scales `total` by `sqrt(target_lambda_weight)` before fitting (and unscales after) — the standard trick for asymmetric per-output weighting in a loss that otherwise weights every dimension's squared error equally, since CatBoost's `MultiRMSE` has no native per-dimension weight parameter. `cv_harness.py` untouched beyond threading 2 new params into the existing `ScorePredictor(...)` call — no changes to fold logic, `naive_baseline_metrics`, or `compute_composite_score`.
- Correctness check: `target_formulation_home_away`, full 5-fold CV, reproduced `champion_cv_baseline_post_injury_fix` **exactly** — val_score_mean 1.3851, per-fold `1.4407,1.3878,1.3796,1.3560,1.3613`, identical to 4dp on every logged metric. Confirms the refactor is genuinely a no-op in the default mode, not just in unit tests.
- Result: `target_formulation_diff_total`, full 5-fold CV. val_score_mean **1.3838** vs. home_away's 1.3851 (Δ−0.0013). Per-fold val delta (diff_total − home_away): fold1 −0.0036, fold2 −0.0008, fold3 −0.0016, fold4 −0.0006, fold5 −0.0001 — **every single fold improves**, not just fold1. Test scores also mostly favor diff_total (mean 1.3694 vs. 1.3727), informational only per the hard constraint.
- Conclusion: **this is the first candidate in this project's CV-protocol history to cleanly clear the per-fold guardrail** — unanimous improvement across all 5 folds, full CV, not a fold1-driven artifact. Magnitude is modest (0.0013 on val_score_mean) but real and consistent.
- Re-audit before adoption (requested explicitly, given this changes the training loss): (1) re-derived the `_to_training_targets`/`_from_training_targets` round-trip algebra by hand; (2) confirmed CatBoost's `MultiRMSE` loss formula against official docs — `sqrt(Σᵢ Σ_d (pred−true)² · wᵢ) / Σᵢ wᵢ`, only a per-sample weight exists, no per-dimension weight (also confirmed by CatBoost GitHub issue #2906, an open feature request for per-dimension weighting *because it doesn't exist yet*) — this is exactly what the `sqrt(lambda)` scaling trick relies on; (3) empirically swept `target_lambda_weight` (0.01/0.5/50.0) on synthetic data and confirmed `total_mae`/`diff_mae` trade off monotonically in the expected direction; (4) re-verified determinism specifically for `diff_total` mode on real fold5 data (two independent runs, full float64 precision, byte-identical — the earlier determinism proof only covered `home_away` mode); (5) found and closed a real gap — `predict_game.py` loads models via `ScorePredictor.load()` for live prediction, and the save/load round-trip for `target_formulation`/`target_lambda_weight` had never been tested; verified it round-trips correctly, added as a permanent regression test; (6) diffed `target_formulation_home_away`'s full-CV row against `champion_cv_baseline_post_injury_fix` on **every** logged column (not just val_score_mean) — exact match throughout.
- **ADOPTED.** `configs/config.yaml`'s `model.target_formulation` flipped to `diff_total` — new standing champion, logged as `champion_cv_baseline_diff_total` (a fresh `run_name`, matching the `champion_cv_baseline`/`champion_cv_baseline_post_injury_fix` naming convention — not left under the experiment's own comparison-arm name). Values are byte-identical to `target_formulation_diff_total`'s row, not re-executed: `diff_total` mode's determinism is proven, so this row *is* what any future run of the now-adopted config would produce.
- Next: `target_lambda_weight` itself was never swept (only the champion's existing `lambda_weight=0.5` composite-score weighting was carried through) — see the `target_lambda_weight_0.75` entry below.

---

**`target_lambda_weight_0.75`** (manual one-off, `scripts/sweep_target_lambda_weight.py`)
- Hypothesis (section 3.3 follow-up): `target_lambda_weight=0.5` was never independently tuned for the `diff_total` training loss — it was only ever inherited from `compute_composite_score`'s own diff/total weighting (a metric-level choice, not a loss-fitting one). A different value might fit `MultiRMSE` in a way that better serves the composite score.
- Stage 1, cheap screen (folds 3–5), grid `[0.1, 0.25, 0.5, 0.75, 1.0, 2.0]`:

  | λ | mean val_score (folds 3-5) |
  |---|---:|
  | 0.1 | 1.3673 |
  | 0.25 | 1.3624 |
  | 0.5 (champion) | 1.3649 |
  | **0.75** | **1.3613** |
  | 1.0 | 1.3656 |
  | 2.0 | 1.3616 |

  0.75 best on screen; not a monotonic trend (0.1 and 1.0 both worse than 0.75 and 2.0), so this reads as a shallow, fairly flat optimum around 0.5–2.0 rather than a sharp one — still enough signal to promote 0.75 to a full-CV check per the escalation protocol.
- Stage 2, full 5-fold CV, `0.5` (reference, not re-logged — identical to `champion_cv_baseline_diff_total`'s already-proven-deterministic numbers) vs. `0.75`:

  | fold | 0.5 | 0.75 |
  |---|---:|---:|
  | 1 | 1.4371 | **1.4368** |
  | 2 | **1.3870** | 1.3898 |
  | 3 | 1.3779 | **1.3719** |
  | 4 | 1.3554 | **1.3527** |
  | 5 | 1.3612 | **1.3592** |
  | **mean** | 1.3838 | **1.3821** |

  0.75 wins val_score on 4 of 5 folds (only fold2 regresses, +0.0028) — not fold1-driven (fold1 also improves, by a small margin). Test-score mean is flat-to-slightly-worse (1.3705 vs. 1.3694), expected noise, not a selection criterion.
- Conclusion: a real, mostly-consistent improvement — smaller magnitude than the `diff_total` reformulation itself (which was 5/5 folds), but clears the "not just one fold" bar with a clean majority.
- **ADOPTED.** `configs/config.yaml`'s `model.target_lambda_weight` flipped to `0.75` — new standing champion is the `target_lambda_weight_0.75` row above. Unlike the two prior supersessions, no separate `champion_cv_baseline_*` row was added: that convention was only ever about giving the CSV a self-describing name, but it's pure duplication when the experiment's own row already carries the winning values — this doc's "Champion baseline" line (section 1) is the actual pointer.
- Next: none — closed.

---

**`a1_fingerprint_freshness_box_score_parity`** (manual one-off, Track A item 1 of `docs/NEXT_PHASE_SESSIONS.md`)
- Hypothesis: `docs/PIPELINE_AUDIT.md`'s findings #2/#3 — the `style_fingerprint` cache (`feature_builder.py:896`) has no freshness check vs. the raw DB, and `box_scores.py`'s parity check only verifies a game_id has *at least one* cached row, not exactly two (home+away) — are both real, currently-dormant safeguard gaps worth closing before they can silently degrade the #1-importance feature family.
- Empirical check first (audit's own instruction): cache max game_date (`matchup_fingerprints`, 2026-05-24) vs. raw DB regular-season max (`game` table, 2026-04-12) — cache still ahead, not stale. `build_box_score_cache()` re-run live: 12,793/12,793 games have exactly 2 rows, 0 incomplete, 0 missing — box-score parity is clean today. Both gaps are exactly what the audit said: real wiring gaps, not live bugs.
- Fix 1 — `feature_builder.py`: added `FeatureBuilder._warn_if_style_fingerprint_cache_stale`, called right after the existing `cache_db.exists()` check in `_add_style_fingerprint_features`. Compares `MAX(game_date)` in `matchup_fingerprints` against `MAX(game_date)` in `nba_api.sqlite`'s `game` table (`season_type='Regular Season'`); logs a warning (not a hard failure — the asof lookup already degrades gracefully to each team's last-cached fingerprint, so staleness alone isn't fatal, just now visible instead of undetectable). Verified the warning path fires correctly against a synthetic stale cache, and is silent on the real (currently-fresh) cache.
- Fix 2 — `box_scores.py`: replaced `_cached_game_ids()` (distinct game_id presence) with `_cached_game_id_counts()` (row count per game_id). `build_box_score_cache()` now treats a game with 0 *or* 1 cached rows as needing (re-)fetch (previously a 1-row game was wrongly treated as "already cached" and would never self-heal on a later run — the actual silent-incompleteness bug the audit flagged), and the parity check now requires exactly 2 rows per game_id (`parity_ok = n_missing == 0 and n_incomplete == 0`), reporting `n_incomplete_in_cache`/`incomplete_sample` as a new, distinct category from `n_missing_in_cache`.
- Both fixes are safeguard/detection additions only — no feature-value computation changed, confirmed by design (read-only diagnostic queries, no change to the merge_asof lookup or to what gets inserted into `box_score_stats` beyond now also covering the 1-row case).
- Full 5-fold CV: mean val_score 1.3824 vs. champion (`target_lambda_weight_0.75`) 1.3821 — Δ+0.0003, not a regression. Per-fold: fold1 1.4368 (exact match), fold2 1.3900 (+0.0002), fold3 1.3742 (+0.0023), fold4 1.3491 (−0.0036), fold5 1.3617 (+0.0025). Given both changes are read-only/diagnostic and provably no-ops on the current (already-fresh, already-100%-parity) data, this small jitter most likely reflects incremental raw-data refresh (new games fetched into `nba_api.sqlite` since the champion row was logged 2026-08-11) rather than anything introduced this session — not investigated further, as it's within the "essentially flat" range this project already treats as noise-level (cf. the `target_lambda_weight` sweep's own flat 1.3841–1.3852 grid).
- `market_benchmark.py` run (`tag=a1_fingerprint_freshness_box_score_parity`): no prior baseline row existed in `outputs/market_benchmark_summary.csv` in this working copy to diff against (first logged run) — headline numbers only: model diff_mae 11.49 (market 10.81), win_acc 0.674 (market 0.694), brier 0.209 (market 0.194). This run's row is now the baseline for future A2–A4 comparisons.
- Conclusion: both audit findings fixed as safeguards (cache-freshness now surfaced via a warning, box-score parity now correctly requires 2/2 rows and self-heals partial fetches). Not adopted/rejected in the ablation sense — this is a correctness/observability fix to existing infrastructure, not a new feature or config flag, so there's nothing to gate behind `enabled: false`.
- Next: Track A item 2 (venue-blind overall-form feature).

---

**`a2_venue_blind_overall_form`** (manual one-off, Track A item 2 of `docs/NEXT_PHASE_SESSIONS.md`)
- Hypothesis: `docs/PIPELINE_AUDIT.md`'s finding #1 — `home_team_win_pct_L{w}`/`diff_avg_L{w}` in `_add_rolling_features` are computed by grouping on `HOME_TEAM_ID`/`AWAY_TEAM_ID` separately, so they're each team's form in that *role* only, not overall recent form. No venue-blind form feature existed anywhere in the model.
- Fix: added `{home_team,away_team}_{win_pct,diff_avg}_overall_L{5,10,20}` (12 new columns, 127→139 total) to `feature_builder.py`'s `_add_rolling_features`, alongside the existing venue-scoped features (not replacing them). Computed via a team-perspective long frame (each game contributes one row per team, win/point-diff from that team's perspective regardless of home/away role), `shift(1).rolling(w).mean()` per team, then merged back onto both the home-row and away-row perspective of each game — same long-frame pattern already used in `_add_opponent_quality_features`. Point-in-time safety verified: `shift(1)` before `rolling`, and a new regression test (`tests/test_overall_form_venue_blind.py`) checks both that the venue-blind value correctly mixes a team's home and away games (differs from the venue-scoped feature on a synthetic fixture) and that a team's first-ever game is NaN (no leakage). Full suite: 186/186 passing.
- Full 5-fold CV: mean val_score 1.3805 vs. A1 baseline 1.3824 — Δ−0.0019 (improvement). Per-fold: fold1 1.4339 (−0.0029), fold2 1.3878 (−0.0022), fold3 1.3742 (0.0000), fold4 1.3512 (+0.0021), fold5 1.3556 (−0.0061). 4 of 5 folds improve or flat, fold4 regresses slightly — net improvement, small magnitude, similar in size to the A1 jitter (not clearly distinguishable from that noise floor on this evidence alone).
- `market_benchmark.py` run (`tag=a2_venue_blind_overall_form`, fold5) vs. A1 baseline row: model diff_mae 11.43 (A1: 11.49, market 10.81 both), total_mae 15.45 (A1: 15.40, slightly worse), win_acc 0.672 (A1: 0.674, flat), brier 0.2077 (A1: 0.2093, improved). Small, mixed movements consistent with the CV result.
- `run_family_importance.py` (`tag=a2_venue_blind_overall_form`, mean across 5 folds): `_add_rolling_features` (the family the new venue-blind columns were added to, alongside the pre-existing venue-scoped ones) ranks **#3 of 13 families**, 16.1% importance share, +0.0137 permutation delta — comfortably inside the top ~6 (`_add_style_fingerprint_features` 29.0%, `_add_style_features` 18.9%, `_add_rolling_features` 16.1%, `_add_elo_features` 8.3%, `_add_matchup_features` 7.9%, `_add_opponent_quality_features` 7.7%). The family-level number blends old and new columns (no per-column breakdown available from CV-protocol runs, only `--protocol single_split` writes `full_feature_importance_*.csv`), so this confirms the *family* remains a top contributor post-addition, not that the 12 new columns specifically are pulling weight — the CV/benchmark deltas above are the real signal for that.
- Conclusion: small, mostly-consistent CV improvement (4/5 folds) and a mixed-but-net-flat-to-positive market_benchmark read. Not a clear win on the scale of past adoptions (e.g. `target_lambda_weight_0.75`'s 4/5-fold win was a larger, cleaner margin), but no regression either, and it closes a real representation gap the pipeline audit flagged. **ADOPTED as an always-on structural feature** — added directly in `_add_rolling_features` (not gated behind a config flag, consistent with how the existing venue-scoped rolling features and other core families like `_add_opponent_quality_features` are unconditional, not opt-in `enabled: false` experimental modules).
- Next: Track A item 3 (`second_of_b2b`).

---

**`a3_rest_venue_blind_fix`** (manual one-off, Track A item 3 of `docs/NEXT_PHASE_SESSIONS.md`)
- Task as scoped: add a `second_of_b2b` indicator, checking first whether existing rest-day features already partially capture it.
- Investigation before implementing: `_add_rest_features`'s `rest_days`/`back_to_back`/`games_in_4_nights` were computed via `df.groupby(team_col)["GAME_DATE"].diff()` where `team_col` is `HOME_TEAM_ID` or `AWAY_TEAM_ID` — i.e. venue-scoped, the same bug class A2 fixed for rolling form. `home_team_rest_days` only looked back to a team's last *home* game, silently skipping any interleaved away game. Verified empirically against real data (`data/raw/nba_api.sqlite`, 12,793 games): 45.3% of home-role rows had a wrong `rest_days` value, and `back_to_back` missed 866 true back-to-backs (6.8% of home rows) — always false negatives (venue-scoped gaps can only overestimate true rest, never underestimate it), never false positives.
- This directly answers the task's own instruction: once `rest_days` is computed correctly (venue-blind), `back_to_back` (`rest_days == 1`) is *exactly* "this is the second game of a back-to-back" for that team, by construction — a team's first-of-b2b game has no way to know a next game is coming without look-ahead, so it's never flagged, and the second game's `rest_days` is definitionally 1. A separate `second_of_b2b` column would be a pure duplicate of a correctly-computed `back_to_back`. **No new column added** — fixed the existing computation instead.
- Fix: rewrote `_add_rest_features` (`feature_builder.py`) to use the same team-perspective long-frame pattern as A2's overall-form fix and `_add_opponent_quality_features` — one row per team-game (home or away), sorted by team then date, `GAME_DATE.diff()` computed once per team across both roles, then merged back onto the home/away perspective of each game. Column names/count unchanged (`{prefix}_rest_days`, `{prefix}_back_to_back`, `{prefix}_games_in_4_nights}`, 139 cols total, same as A2) — this is a correctness fix to existing always-on features, not a new feature.
- Verification: rebuilt the full feature frame and diffed every value against an independently-computed venue-blind ground truth (same long-frame logic, written standalone) — exact match, 0 mismatches, both `rest_days` and `back_to_back`, both home and away columns. New regression test `tests/test_rest_features_venue_blind.py`: (1) a team playing away on day 1 then home on day 2 (a back-to-back spanning both venue roles) is now correctly flagged `back_to_back=1`/`rest_days=1` — the venue-scoped bug would have missed this entirely; (2) a team's first-ever game still defaults `rest_days=3`/`back_to_back=0` (unchanged fillna convention, no leakage). Full suite: 188/188 passing.
- Full 5-fold CV: mean val_score **1.3811** vs. champion (`target_lambda_weight_0.75`) 1.3821 — Δ−0.0010 (improvement). Per-fold delta (new − champion): fold1 1.4345 (−0.0023), fold2 1.3883 (−0.0015), fold3 1.3720 (+0.0001), fold4 1.3522 (−0.0005), fold5 1.3586 (−0.0006). 4 of 5 folds improve, fold3 flat (negligible +0.0001) — not fold1-driven, clears the per-fold guardrail.
- `market_benchmark.py` run (`tag=a3_rest_venue_blind_fix`, fold5) vs. A2 baseline row: model diff_mae 11.41 (A2: 11.43, improved), total_mae 15.37 (A2: 15.45, improved), win_acc 0.674 (A2: 0.672, improved), brier 0.2080 (A2: 0.2077, essentially flat, +0.0002). Small, mostly-positive movement, consistent with the CV result.
- Conclusion: a real, previously-undetected correctness bug in a core always-on feature family (parallel to A2's finding, same root cause — venue-role groupby silently excluding a team's games in the other role), now fixed with a clean, mostly-consistent CV/benchmark improvement. Confirms the task's own hypothesis: the existing `back_to_back` feature already exactly captures "second game of a back-to-back" once computed correctly — a dedicated `second_of_b2b` column would have added a redundant, perfectly-correlated duplicate feature for no benefit. **ADOPTED as a bug fix** — no config flag, since this corrects existing always-on features rather than introducing a new one (same treatment as A1/A2).
- Next: Track A complete (3/3). Per `docs/NEXT_PHASE_SESSIONS.md`, next is Track B item 0 (representation inventory, read-only) — A4 (VIF trim) stays deferred until after Track B settles.

---

**`b1_style_features_decay_weighted` / `b1_style_and_rolling_decay_weighted`** (manual one-off, Track B item 1 of `docs/NEXT_PHASE_SESSIONS.md`, per `docs/FEATURE_REPRESENTATION_AUDIT.md` priorities #1-2)
- Hypothesis: `style_features`/`rolling_features` are mean-only (or volume-weighted-ratio-only) at every window (L5/L10/L20), while `style_fingerprint_features` (the #1-importance family) already uses a decay-weighted mean (`fingerprint.py`'s `_decayed_weighted_mean`, `halflife=13.199390932957819`) and outperforms them. Swapping the flat mean for that same, already-proven decay-weighting — reused as-is, same halflife, no new construction — might close that gap. Added as NEW columns alongside the existing flat-mean ones (not replacing), so the two representations could be compared directly. Two sequential steps to separate each family's own contribution: step 1 = `style_features`' `off_eff_L{w}`/`def_eff_L{w}` only (12 new cols, `home_/away_team_{off,def}_eff_decay_L{5,10,20}`); step 2 = also `rolling_features`' `win_pct_L{w}`/`diff_avg_L{w}` (venue-scoped) and `win_pct_overall_L{w}`/`diff_avg_overall_L{w}` (venue-blind) (24 more new cols). `fg3_pct`/`fg_pct`/`ft_pct` deliberately excluded from both families — those are volume-weighted ratios (`sum(made)/sum(att)`), not flat means, so the swap doesn't apply the same way; decay-weighting a ratio's numerator/denominator sums would be new construction, not the "reuse `_decayed_weighted_mean` as-is" swap this session was scoped to.
- Step 1 full 5-fold CV (`b1_style_features_decay_weighted`, 151 cols): mean val_score **1.3814** vs. baseline (`a3_rest_venue_blind_fix`) 1.3811 — Δ+0.0003. Per-fold delta (new − baseline): fold1 +0.0067, fold2 −0.0074, fold3 +0.0026, fold4 −0.0029, fold5 +0.0024 — 2 of 5 folds improve, 3 regress, no consistent direction. `market_benchmark.py` (`tag=b1_style_features_decay_weighted`, fold5) vs. baseline: diff_mae 11.429 (baseline 11.412, worse), total_mae 15.344 (baseline 15.366, better), win_acc 0.6689 (baseline 0.6738, worse), brier 0.2077 (baseline 0.2080, essentially flat). Mixed, no clear direction — reads as a **null result**.
- Step 2 full 5-fold CV (`b1_style_and_rolling_decay_weighted`, 175 cols, cumulative with step 1): mean val_score **1.3832** vs. baseline 1.3811 (Δ+0.0021, cumulative) / vs. step 1's 1.3814 (Δ+0.0018, `rolling_features`' own incremental contribution). Incremental per-fold delta (step 2 − step 1): fold1 −0.0007, fold2 +0.0036, fold3 +0.0011, fold4 +0.0063, fold5 −0.0011 — 3 of 5 folds regress, magnitude larger than step 1's noise floor. Cumulative per-fold delta (step 2 − baseline): fold1 +0.0060, fold2 −0.0038, fold3 +0.0037, fold4 +0.0034, fold5 +0.0013 — 4 of 5 folds regress. `market_benchmark.py` (`tag=b1_style_and_rolling_decay_weighted`, fold5) is unanimous: diff_mae 11.492 (baseline 11.412, step 1 11.429 — worse than both), total_mae 15.485 (baseline 15.366, step 1 15.344 — worse than both), win_acc 0.6598 (baseline 0.6738, step 1 0.6689 — worse than both), brier 0.2096 (baseline 0.2080, step 1 0.2077 — worse than both). Every metric moves the wrong way relative to both comparators — this is a **consistent, if modest, regression**, not a null result.
- Diagnosis (single_split run, `outputs/full_feature_importance_b1_step2_importance_check.csv`, `--no-log`, diagnostic only): 7 of the top-20 CatBoost-importance features are the new `style_features` decay columns (`home_team_off_eff_decay_L20` rank 6, `away_team_off_eff_decay_L10` rank 7, etc. — all 12 style decay cols land in the top 33), while none of the 24 `rolling_features` decay columns crack the top 40 (venue-scoped `win_pct_decay`/`diff_avg_decay`: ranks 78-152 of 175; venue-blind `*_overall_decay`: ranks 45-103). Checked pairwise correlation between each decay column and its flat-mean original on the training features: 0.985-0.998 across every pair sampled (`off_eff_L20` vs. `off_eff_decay_L20`, `win_pct_L10` vs. `win_pct_decay_L10`, etc.) — near-perfect collinearity, expected given `halflife=13.2` games is long relative to L5/L10/L20 windows, so decay-reweighting barely perturbs the ranking within a window. This explains both results at once: CatBoost happily splits importance across near-duplicate columns (hence style's high top-20 presence) without that translating into genuine new signal (hence step 1's null CV/benchmark read), and for `rolling_features` specifically the added collinear columns net out as pure model complexity with no offsetting benefit — consistent with step 2's real regression.
- Conclusion: **rejected, both steps.** Step 1 (`style_features`) is a clean null — no evidence either way, consistent with the mechanistic finding that these columns are near-duplicates of existing ones. Step 2 (`rolling_features`) is a real, small, consistently-signed regression across both CV (4/5 folds) and every `market_benchmark` metric — worse than a null, not just inconclusive. Per this project's adopt-only-on-positive-evidence norm (cf. A2/A3/`target_lambda_weight_0.75`, all adopted on a clear majority-of-folds improvement), neither step clears the bar. **Code reverted** — `src/feature_engineering/feature_builder.py` restored to its pre-session state (`git checkout`), no decay-weighted columns left in the always-on feature set. The two CV rows and both `market_benchmark` rows stay in `outputs/experiments_v2.csv`/`outputs/market_benchmark_summary.csv` as permanent negative-result evidence per this doc's archival convention, so a future session doesn't have to re-run this test to learn it doesn't work.
- Cross-cutting finding for the family-representation question broadly: "family X's importance is high" and "family X's current representation is under-capturing its information" are not the same claim, and this session is a direct empirical counterexample to assuming the latter from the former — `style_fingerprint_features`' decay-weighting works well for its own metrics but transplanting the identical function onto `style_features`/`rolling_features`' columns doesn't help, most likely because those columns' *existing* flat means already sit inside a `halflife=13.2` neighborhood of the decay-weighted version, so there's little independent information left for a second, highly-correlated view of nearly the same rolling window to add. This is a specific instance of the same collinearity concern A4 (VIF trim, still deferred until Track B settles) is meant to catch — worth keeping in mind if any *future* B-series candidate (e.g. any richer-representation idea for a family whose columns are known to already sit inside a similar decay/window neighborhood) is proposed.
- Next: per `docs/FEATURE_REPRESENTATION_AUDIT.md`'s priority order, `elo_features` (priority #3) is next in the B-series queue — see the status log entry in `docs/NEXT_PHASE_SESSIONS.md` for whether this result changes its scope.

---

**`b2_elo_momentum` / `b2_elo_momentum_and_volatility`** (manual one-off, Track B item 2 of `docs/NEXT_PHASE_SESSIONS.md`, per `docs/FEATURE_REPRESENTATION_AUDIT.md` priority #3)
- Hypothesis: `elo_features` had zero volatility/rate-of-change representation — only the point-in-time running rating (`home_team_elo`/`away_team_elo`/`elo_diff`). Unlike B1's swap, no existing function to reuse — new construction, from scratch. Two candidates, sequenced (step 1 in isolation, step 2 cumulative, same methodology as B1, so each candidate's own contribution is separable): step 1 = `elo_momentum_L{5,10,20}` (net Elo rating change over a team's last `w` games, home/away/diff, 9 new cols); step 2 = also `elo_volatility_L{5,10,20}` (rolling std, `min_periods=2`, of a team's game-to-game rating deltas, home/away/diff, 9 more new cols). Both computed via a new team-perspective long frame in `src/feature_engineering/elo.py` (`compute_elo_momentum`, `compute_elo_volatility`), fed `compute_elo_ratings`'s already-point-in-time-safe pre-game ratings. Leakage-safety note (verified explicitly, since this is new construction, not reuse of an already-audited function like B1): `compute_elo_ratings`'s pre-game ratings are *already* "strictly before this game" by construction (unlike raw per-game box-score stats elsewhere in this codebase, which need `shift(1)` before rolling) — so `pre_game_elo[k] - pre_game_elo[k-w]` (momentum) and a rolling std over `pre_game_elo.diff()` (volatility) need no additional shift; both only ever reference games strictly before the row being featurized. Tested directly (no DB fixture needed, since both functions take `elo_ratings` as a plain argument): `tests/test_elo_momentum.py` (4 tests — exact value match, NaN before `w` prior games exist, boundary-exact value at exactly `w` games, and a perturbation test proving no row's momentum changes when a *later* game's rating changes).
- Per this session's own instruction (a direct lesson from B1): correlation with `elo_diff` computed **before** running CV, on the full training set. `elo_momentum_diff_L{w}` vs. `elo_diff`: L5 0.198, L10 0.269, L20 0.365 — low-to-moderate, nothing like B1's 0.985-0.998 near-duplicates. `elo_volatility_diff_L{w}` vs. `elo_diff`: L5 0.005, L10 −0.018, L20 −0.059 — essentially zero. `elo_volatility_diff_L{w}` vs. `elo_momentum_diff_L{w}` (same window): 0.06-0.10 — weak. All three new axes are genuinely close to orthogonal to what the model already sees, ruling out B1's collinearity mechanism up front for both candidates.
- Step 1 full 5-fold CV (`b2_elo_momentum`, 148 cols): mean val_score **1.3803** vs. baseline (`a3_rest_venue_blind_fix`) 1.3811 — Δ**−0.0008** (improvement). Per-fold delta (new − baseline): fold1 −0.0009, fold2 +0.0005, fold3 −0.0012, fold4 −0.0043, fold5 +0.0019 — 3 of 5 folds improve, 2 regress only slightly (largest regression +0.0019, well inside this project's established noise floor). `market_benchmark.py` (`tag=b2_elo_momentum`, fold5) leans positive: win_acc 0.6795 (baseline 0.6738, **better**), total_mae 15.357 (baseline 15.366, better), brier 0.2078 (baseline 0.2080, flat/better), diff_mae 11.430 (baseline 11.412, slightly worse). Diagnostic single_split importance run: `home_team_elo_momentum_L20` ranks 26/148, `away_team_elo_momentum_L10` ranks 40/148 — just outside the top-20 but a reasonable placement for a brand-new construction inside an 8.3%-share family.
- Step 2 full 5-fold CV (`b2_elo_momentum_and_volatility`, 157 cols, cumulative with step 1): mean val_score **1.3816** vs. baseline 1.3811 (Δ+0.0005, cumulative — net roughly flat) / vs. step 1's 1.3803 (Δ**+0.0013**, volatility's own incremental contribution — a regression that erodes most of step 1's gain). Incremental per-fold delta (step 2 − step 1): fold1 +0.0009, fold2 −0.0034, fold3 **+0.0099**, fold4 −0.0008, fold5 −0.0001 — 2 improve, 2 regress, 1 flat, with fold3's regression alone larger than any single-fold movement either B1 step showed. `market_benchmark.py` (`tag=b2_elo_momentum_and_volatility`, fold5) is unanimous the wrong way: diff_mae 11.476 (worse than both baseline 11.412 and step 1 11.430), total_mae 15.499 (worse than both 15.366/15.357), win_acc 0.6623 (worse than both 0.6738/0.6795 — worse than even the pre-elo-momentum baseline), brier 0.2089 (worse than both 0.2080/0.2078).
- Diagnosis: this is a **different failure mode from B1**, not the same collinearity story. Volatility columns are NOT collinear with `elo_diff` or momentum (correlations checked above, near-zero) and DO carry real CatBoost split-importance on their own once added (`away_team_elo_volatility_L10` ranks 23/157, `away_team_elo_volatility_L20` ranks 33/157 — comparable to or better than momentum's own step-1 placement). So this isn't "CatBoost splitting credit across a near-duplicate that adds nothing" — it's a real, non-trivial in-sample signal that doesn't generalize. Most likely explanation: `std` over only 2-20 delta values (`min_periods=2`) is an inherently high-variance estimator at these window lengths — a small-sample-statistics problem, not a construction bug (the leakage-safety and correlation checks above rule out both of those). Adding volatility also visibly diluted momentum's own importance in the same run (`elo_momentum_diff_L10` fell to rank 57/157 from momentum-alone's rank-26 in step 1) — the two axes share just enough of the same underlying "how much has this team's rating recently moved" information (weak but nonzero 0.06-0.10 correlation) that CatBoost redistributes credit between them without a net benefit.
- Conclusion: **momentum ADOPTED, volatility REJECTED.** Momentum clears this project's adoption bar the same way A2/A3/`target_lambda_weight_0.75` did — majority-of-folds CV improvement (3/5, none catastrophic), a market_benchmark read that leans positive on 3 of 4 metrics, and (crucially, unlike B1) low correlation with the existing `elo_diff` confirming it's real distinct information rather than a redundant near-duplicate. Volatility does not — a real incremental regression on CV (driven substantially by one large single-fold miss) and a unanimous market_benchmark regression on every metric, despite passing the pre-CV collinearity check that would have flagged a B1-style outcome. **Adopted as an always-on structural feature, momentum only**: `src/feature_engineering/feature_builder.py`'s `_add_elo_features` keeps the `compute_elo_momentum` call (9 new columns, 139→148 total); `compute_elo_volatility` and its wiring were removed from `src/feature_engineering/elo.py`/`feature_builder.py` after the step-2 result came in (`tests/test_elo_volatility.py` deleted alongside it — no code path exercises it once unwired). `tests/test_elo_momentum.py` (4 tests) stays as the permanent leakage-safety regression test for the now-adopted feature. Both CV rows and both `market_benchmark` rows stay in `outputs/experiments_v2.csv`/`outputs/market_benchmark_summary.csv` — the step 2 row is a real, useful negative-result data point (volatility's rejection reason) even though its code isn't in the live feature set.
- Cross-cutting finding: a low pre-CV correlation check (this session's own methodology, adopted directly from the B1 postmortem) is necessary but **not sufficient** to predict whether a new feature will generalize — it rules out one specific failure mode (collinear redundancy) but not another (a real, low-collinearity signal that's simply too noisy at the tested window lengths to generalize). Both checks — correlation up front, and a post-hoc importance/diagnosis pass when the CV result doesn't match the correlation read — earned their keep in the same session on two different candidates.
- This was the last priority family from the B0 inventory (`style_features`+`rolling_features` bundled priority #1-2, `elo_features` priority #3; `opponent_quality_features`/`matchup_features` deprioritized, `style_fingerprint_features` excluded). A Track B rollup (per `docs/NEXT_PHASE_SESSIONS.md`'s B-final template) is now appropriate to decide next steps — not run in this session per its own scope (elo only, no rollup).
- Next: Track B rollup (cumulative pre-Track-B vs. now, `elo_momentum` being the only surviving B-series addition) — a separate session, per `docs/NEXT_PHASE_SESSIONS.md`'s explicit "STOP — do not proceed to A4 or Track C without a separate human decision" instruction for that step.

---

**`a4_vif_trim_overall_form`** (manual one-off, Track A item 4 of `docs/NEXT_PHASE_SESSIONS.md`, run on the post-Track-B live feature set)
- Diagnostic (`scripts/full_feature_vif.py`, new — extends `family_correlation_vif.py`'s methodology from 3 families to the full 148-column live feature set, same fold5-train-features/correlation-matrix-inverse VIF technique): 22/148 columns exceed VIF=10. By far the largest, cleanest cluster: `_add_rolling_features`' venue-blind "overall" `win_pct`/`diff_avg` columns (added in `a2_venue_blind_overall_form`) at `L10`/`L20` — 8 columns, VIF 19.46–36.49, the top 8 entries in the whole ranking. The same block's own `L5` columns were the only window *not* flagged (VIF 9.06–9.63, just under the cutoff) — consistent with the underlying mechanism: as the venue-blind window grows past ~10 games it increasingly overlaps both the pre-existing venue-scoped `win_pct`/`diff_avg` at the same window length and its own longer/shorter siblings, since a team's last 10–20 games regardless of venue and its last 10–20 games in one specific venue role draw from much the same recent stretch of the season. Other flagged columns (not acted on this session, see below): `style_fingerprint_features`' `offensive_rating`/`defensive_rating` (VIF 13.09–17.14), venue-scoped `win_pct_L10`/`fg_pct_L10` (11.32–13.26), `opponent_quality_features`' `opp_def`/`opp_off_quality_L20` (10.08–10.90), raw `elo_features`' `home_team_elo`/`away_team_elo` (10.38–10.39).
- Trim tested (single, targeted candidate — the highest-VIF cluster only, not an attempt to address all 22 flagged columns in one session): dropped `{home,away}_team_{win_pct,diff_avg}_overall_L{10,20}` (8 cols), kept `_overall_L5` and every venue-scoped column unchanged. `_add_rolling_features` restricted to `overall_windows = [min(self.rolling_windows)]` for the venue-blind block only. 148→140 features. Full test suite: 192/192 passing.
- Full 5-fold CV (`a4_vif_trim_overall_form`): mean val_score **1.3801** vs. baseline (`b2_elo_momentum`) 1.3803 — Δ−0.0002, essentially flat. Per-fold delta (trim − baseline): fold1 +0.0033, fold2 −0.0053, fold3 +0.0022, fold4 +0.0029, fold5 −0.0041 — **3 of 5 folds regress** (1, 3, 4), only 2 improve (2, 5); the tiny net mean improvement is driven by 2 larger-magnitude improving folds outweighing 3 smaller regressions, not by a fold-majority pattern. Fails this project's per-fold guardrail (folds 2–5 must show the improvement; here 2 of those 4 improve and 2 regress — a genuine 50/50 split, not a majority).
- `market_benchmark.py` (`tag=a4_vif_trim_overall_form`, fold5) vs. `b2_elo_momentum` baseline: diff_mae 11.4296 (baseline 11.4298, flat), total_mae 15.399 (baseline 15.357, **worse**, +0.042), win_acc 0.6787 (baseline 0.6795, worse, −0.0008), brier 0.2083 (baseline 0.2078, worse, +0.0005) — 3 of 4 metrics move slightly the wrong way, 1 flat.
- Conclusion: **not adopted — mixed/null-to-slightly-negative result**, same standard applied to `b1_style_features_decay_weighted` and `E2`'s season_motivation screen (a near-zero or directionally-mixed mean is not itself an adoption signal without a fold/metric majority behind it). **Code reverted** to the pre-trim, post-Track-B state (148 features) — verified the reconstruction reproduces `b2_elo_momentum`'s full 5-fold CV numbers exactly (mean 1.3803, per-fold 1.4336/1.3888/1.3708/1.3479/1.3605, byte-identical to 4dp) before treating the working tree as restored. Both the CV row and the `market_benchmark` row stay in `outputs/experiments_v2.csv`/`outputs/market_benchmark_summary.csv` as permanent negative-result evidence, per this doc's archival convention. The other 14 flagged-but-untested columns (style_fingerprint ratings, raw elo levels, opponent-quality `L20`, venue-scoped `win_pct_L10`/`fg_pct_L10`) are lower-confidence, structurally more foundational candidates (raw elo ratings and style-fingerprint ratings in particular feed multiple derived families) — left flagged for a future session's own judgment call, not trimmed here, consistent with this project's one-hypothesis-per-session discipline.
- **Phase-wide cumulative delta, pre-Track-A baseline (`target_lambda_weight_0.75`, 127 features) through Track A + Track B + this (rejected) trim** — since A4 wasn't adopted, the final state of the whole phase is unchanged from the Track B rollup, i.e. `b2_elo_momentum` (148 features):
  - CV: val_score_mean 1.3821 → 1.3803, Δ**−0.0018** (improvement). Per-fold delta: fold1 −0.0032, fold2 −0.0010, fold3 −0.0011, fold4 −0.0048, fold5 +0.0013 — 4 of 5 folds improve, fold5 regresses slightly. Test score (informational only): 1.3705 → 1.3661, Δ−0.0044.
  - `market_benchmark` (fold5, `a1_fingerprint_freshness_box_score_parity` — 127 features, the earliest logged market_benchmark row and the same config as the pre-Track-A CV baseline — vs. `b2_elo_momentum`): diff_mae 11.487→11.430 (better), total_mae 15.403→15.357 (better), win_acc 0.6738→0.6795 (better), brier 0.2093→0.2078 (better) — **all 4 metrics improve**.
  - Feature count: 127 → 148 (+21: +12 `a2_venue_blind_overall_form`, +9 `b2_elo_momentum`; `a3_rest_venue_blind_fix` was a correctness fix with no column-count change).
  - Net read: a real, consistent, modest phase-wide gain (Δ−0.0018 CV, unanimous market_benchmark improvement) built almost entirely from two adopted features across the whole A+B+A4 phase (`a2`'s venue-blind overall form and `b2`'s elo momentum) plus one correctness bug fix (`a3`); every other tested candidate in the phase (B1's style/rolling decay-weighting, B2's elo volatility, this session's VIF-driven overall-form trim) was a rejected null or small regression. Consistent with the Track B rollup's own framing — small and real, not a breakthrough.
- Next: none planned — A4 is closed per this doc's rejection. A future session could revisit the remaining flagged-but-untested VIF columns (style_fingerprint ratings, raw elo levels, `opponent_quality_features` `L20`, venue-scoped `win_pct_L10`/`fg_pct_L10`) individually, but none is queued. Track C (new orthogonal data) remains the next item in `docs/NEXT_PHASE_SESSIONS.md`, pending a separate human decision.

---

**`official_pace_poss_new_columns`** (manual one-off, Track C follow-up per `docs/NEXT_PHASE_SESSIONS.md`, run on the post-Track-B live feature set, per `docs/NEW_DATA_FEASIBILITY.md`'s pace/possession candidate)
- Hypothesis: `docs/NEW_DATA_FEASIBILITY.md` flagged `pace_score` (a box-score-derived proxy, `PTS + OPP_PTS + TOV - 0.44*FTA`) as a candidate for swap-in refinement against nba_api's own computed `PACE`/`POSS` (`TeamGameLogs`, `MeasureType=Advanced`) — a more precise version of the same underlying signal, cheap to fetch (same `nba_api` dependency, season-level bulk calls like `box_scores.py` already uses, not per-game). Added as NEW columns alongside `pace_score`, not a replacement.
- Implementation: new module `src/matchups/pace_possession.py` (mirrors `box_scores.py`'s fetch/cache/parity-check structure exactly — one `TeamGameLogs` call per season/season_type, ~20 calls total, not per-game), new cache table `team_advanced_stats` (`src/matchups/db.py`). `fingerprint.py`'s `_load_raw_team_game_metrics`/`compute_rolling_fingerprints` extended to also compute a decay-weighted rolling mean for `official_pace`/`official_poss` (same window/halflife as every other fingerprint metric), cached into `matchup_fingerprints` (2 new columns via `ALTER TABLE`, same migration pattern as `offensive_rating`). Deliberately kept OUT of `FINGERPRINT_METRICS` and never touched by `injury_layer.py` (Layer 1/uncalibrated only, same scope cut as `offensive_rating` — no archetype's `injury_impact` config references these). `feature_builder.py`'s `_add_style_fingerprint_features` reads them from layer=1 directly, gated by a new, separately-toggleable `style_matchup.official_pace_enabled` flag (default `false`) so this candidate is independently ablatable from the already-adopted 18 raw-fingerprint columns. Adds 6 columns when enabled: `{home,away}_style_official_{pace,poss}` + `style_official_{pace,poss}_diff` (148→154 features). Backfill: 12,793/12,793 games, 100% parity (home+away), ~1 minute wall time — confirms the season-level bulk-call approach (vs. a per-game pull) works exactly as `docs/NEW_DATA_FEASIBILITY.md` anticipated for this specific candidate.
- Pre-CV correlation check (per this session's own instruction, same discipline as `b2_elo_momentum`'s check against `elo_diff`): `official_pace` (rolling, decay-weighted) vs. `pace_score`: **0.72–0.73** — moderate, well below B1's 0.985–0.998 near-duplicate range, so this doesn't rule the candidate out up front the way B1's check would have. But `official_pace` vs. `official_poss` (the two NEW columns against **each other**): **0.976** — a near-duplicate pair by the same B1 threshold, flagged explicitly before running CV as the one correlation number that looked concerning.
- Full 5-fold CV (`official_pace_poss_new_columns`, 154 cols): mean val_score **1.3832** vs. baseline (`b2_elo_momentum`) 1.3803 — Δ**+0.0029** (regression). Per-fold delta (new − baseline): fold1 +0.0022, fold2 +0.0018, fold3 **+0.0105**, fold4 +0.0063, fold5 −0.0064 — **4 of 5 folds regress** (1, 2, 3, 4), only fold5 improves. Not a fold1-driven artifact (fold1's own delta is small; fold3 is the largest single-fold move). Fails the per-fold guardrail decisively — this is a real, consistently-signed regression, not a null.
- `market_benchmark.py` (`tag=official_pace_poss_new_columns`, fold5) vs. `b2_elo_momentum` baseline: diff_mae 11.439 (baseline 11.430, worse), total_mae 15.415 (baseline 15.357, worse), win_acc 0.6828 (baseline 0.6795, better), brier 0.2082 (baseline 0.2078, worse, flat) — 3 of 4 metrics worse, 1 better.
- Diagnosis: single_split diagnostic importance run (`outputs/full_feature_importance_official_pace_smoke_test.csv`) shows `away_style_official_pace` at rank 4/154 and `away_style_official_poss` at rank 7/154 (`home_style_official_pace`/`home_style_official_poss` rank 16/20) — a high in-sample split-importance placement, comparable to or better than `b2_elo_momentum`'s own rank-26 placement at adoption. This is the **same signature B1 found**: high single-split CatBoost importance that does NOT translate into a CV generalization gain. The mechanism, per the pre-CV correlation check, is more specific than B1's though — it isn't that the new columns duplicate an *existing* feature (`pace_score` correlation is only moderate, 0.72–0.73, genuinely below B1's threshold), it's that the **two new columns duplicate each other** (0.976): CatBoost splits importance across both `official_pace` and `official_poss` (near-identical information, since both derive from close variants of the same box-score possession estimate), inflating apparent single-split importance without adding two independent axes of real signal — closer to paying the cost of 6 new columns for the generalizable content of roughly one.
- Conclusion: **rejected — not promoted.** A real, consistently-signed CV regression (4/5 folds) and a `market_benchmark` read that's 3/4 metrics worse, despite passing the pre-CV correlation check against the existing feature (`pace_score`) the way B1's postmortem would have flagged. Confirms the cross-cutting lesson from `b2_elo_momentum_and_volatility`: a low-to-moderate correlation against the *existing* feature set is necessary but not sufficient — collinearity **between the new candidates themselves** (not checked by that framing alone) is a separate failure mode, and this session is the first case where that specific version of the check would have caught the actual problem in advance. `style_matchup.official_pace_enabled` stays `false`. **Code kept, not reverted** (unlike `b1_style_and_rolling_decay_weighted`/`a4_vif_trim_overall_form`, which were pure formula/column-selection changes with no standalone value once rejected): `pace_possession.py`'s collection module and the `team_advanced_stats` cache are a genuine, working, cheap (~1 minute) data source, not a derived reformulation of existing columns — left in place, gated off, on the same footing as `injury_features.missing_value_strategy`'s `native_nan` variant (E4, also rejected, also kept as a disabled option) rather than fully reverted, in case a future session wants to test `official_pace` or `official_poss` alone (not both) or use the cached data for a different derived construction. Both the CV row and the `market_benchmark` row stay in `outputs/experiments_v2.csv`/`outputs/market_benchmark_summary.csv` as permanent negative-result evidence.
- Verification: with `official_pace_enabled` back at `false`, `_add_style_fingerprint_features`'s uncalibrated-metrics branch is provably a no-op (the conditional only appends to an already-equivalent single-element list), confirmed empirically — a single_split run with the flag off produces exactly 148 features (`outputs/full_feature_importance_official_pace_off_verify.csv`, 148 rows), matching the pre-session `b2_elo_momentum` state. Full test suite: 192/192 passing (one pre-existing test fixture, `tests/test_style_fingerprint_features.py`'s `_mock_config()`, needed `official_pace_enabled=False` added explicitly — a bare `MagicMock()` attribute is truthy by default, so the new conditional was silently taking the enabled branch against a fixture DB that predates the migration).
- Next: none planned this session — this closes the pace/possession half of `docs/NEW_DATA_FEASIBILITY.md`'s three candidates as tested-and-rejected (previously the doc's own verdict was "cheap swap-in worth testing if a spare cycle opens up"; now it has a real, negative, CV-backed answer). Confidence read for the remaining shot-quality candidate (`docs/NEW_DATA_FEASIBILITY.md` §3): this session found that even a *moderate* (0.72–0.73), genuinely-not-B1-style correlation against an existing feature wasn't enough to guarantee real signal — a raw shot-zone-mix pull (flagged in that doc as risking overlap with `three_pt_reliance`/`paint_activity`) now carries one more concrete data point suggesting "not an obvious near-duplicate of what exists today" is a necessary but weaker filter than it might seem, and that any multi-column new-data candidate (shot-quality would likely add several zone/quality columns at once, same shape as this session's pair) should also check for collinearity **among its own new columns**, not just against the existing feature set, before running CV.

---

**`opponent_adjusted_efficiency_L5` / `_L10` / `_L20`** (session `20260823_1525_retro-opp-adjust`, `scripts/run_opponent_adjusted_efficiency_sweep.py`, per `docs/NEXT_PHASE_SESSIONS.md` backlog item 5's 2026-08-23 scoping pass)
- Hypothesis: a team's own rolling `off_eff`/`def_eff` reweighted by the quality of opponents already faced during that window (distinct from `opponent_quality_features`, which describes the *upcoming* opponent) adds real signal. Direct extension of `compute_opponent_adjusted_form_scores`' own template (per-game signed residual vs. opponent's pre-game quality, then a `shift(1)` rolling mean) from win/loss to points scored/allowed — new `compute_team_offense_defense_history`/`compute_opponent_adjusted_efficiency_scores` (`season_motivation.py`), 4 new columns (`{home,away}_team_opponent_adjusted_{off,def}_score`, 148→152 features), gated by new `season_motivation.opponent_adjusted_efficiency_enabled` (default `false`) + `_window` (default 10). Point-in-time safety: opponent's pre-game quality is a cumulative (expanding, strictly-before-this-game) average via the same cumcount/cumsum pattern as `win_pct_before`, and the residual itself is additionally `shift(1)`-lagged before the outer rolling mean — same double-lag structure `compute_opponent_adjusted_form_scores` already uses, verified by a new regression test (`tests/test_opponent_adjusted_efficiency.py`, 5 tests: exact residual value, first-game-ever NaN not a fabricated default, rolling mean correctly skips NaN residuals, and a perturbation test proving no row changes when a later game's result changes).
- Given the scoping pass's own flagged risk — the closest precedent (`opponent_adjusted_form_score`, win/loss) passed CV at window=10 only and **inverted sign** at windows 5/15 (`docs/features/season_motivation_log.md` §10) — this session tested **all of L5/L10/L20, full 5-fold CV each**, not a single window, per the task's explicit instruction.
- Pre-CV correlation check (required before CV, same discipline as `b2_elo_momentum`/`official_pace_poss_new_columns`), all 3 windows:
  - New column vs. the existing `off_eff_L{w}`/`def_eff_L{w}` it's derived from: **r=+0.65 to +0.76** (off) / **r=−0.64 to −0.75** (def) — moderate-to-fairly-strong, well above `official_pace`'s 0.72–0.73 (which alone produced a real regression) and below B1's 0.985+ near-duplicate range. A real, non-trivial overlap with an existing feature, flagged as a risk before CV per the scoping pass.
  - New column vs. `opponent_quality_features`' `opp_def_quality_L{w}`/`opp_off_quality_L{w}`: r≈0.14–0.19 — low, not a duplicate of that family.
  - The two new columns against **each other** (off_score vs. def_score, the `official_pace` lesson): r≈−0.34 to −0.36 — modest, not a near-duplicate pair.
- Full 5-fold CV, all 3 windows (baseline `b2_elo_momentum`: mean 1.3803, per-fold 1.4336/1.3888/1.3708/1.3479/1.3605):

  | window | mean val | Δ vs. baseline | fold1 | fold2 | fold3 | fold4 | fold5 |
  |---|---:|---:|---:|---:|---:|---:|---:|
  | L5 | 1.3788 | **−0.0015** | +0.0019 | −0.0115 | +0.0108 | −0.0064 | −0.0022 |
  | L10 | 1.3790 | **−0.0013** | +0.0021 | −0.0091 | +0.0076 | −0.0001 | −0.0070 |
  | L20 | 1.3797 | **−0.0006** | −0.0004 | −0.0066 | +0.0092 | −0.0044 | −0.0009 |

  (deltas = new − baseline, negative = improvement)
- Result, stated plainly: **all 3 windows beat the champion on mean val_score, and all 3 clear the per-fold guardrail independently** — folds 2/4/5 improve at every single window tested, only fold3 regresses at every window (a consistent +0.0076 to +0.0108, not noise-sized), and fold1 is small and direction-mixed (slightly worse at L5/L10, slightly better at L20), so none of the three is fold1-driven. This is the same "majority of folds 2–5 improve, one exception" shape that `target_lambda_weight_0.75` was adopted on (4/5 folds, only fold2 regressed).
- **Does this replicate the closest precedent's window-instability, or behave differently?** Behaves **differently, clearly** — this is explicitly checked, not assumed. `opponent_adjusted_form_score` (win/loss) *inverted sign* between window=10 (passed) and windows 5/15 (both failed, opposite direction) — a full flip in which folds favored the signal. Here, direction is **identical at every window**: the same three folds (2, 4, 5) improve and the same one fold (3) regresses, with magnitude drifting smoothly as the window widens (mean Δ shrinks monotonically from −0.0015 at L5 to −0.0006 at L20) rather than flipping. Also distinct from `official_pace_poss_new_columns` and `b1_style_and_rolling_decay_weighted` (importance/correlation without CV generalization) — here the moderate 0.65–0.76 correlation against `off_eff`/`def_eff` did **not** predict a regression the way it did for `official_pace`'s comparable 0.72–0.73 correlation against `pace_score`. Not a third confirmation of either prior failure pattern.
- fold3's consistent regression (the one reproducible weak point, same direction/magnitude across mechanically different window widths) is worth a name: plausibly a real season/fold-specific characteristic (fold3 = train through 2022-04-10, val 2022-10-18→2023-04-09) rather than an artifact of this feature's construction, since three different rolling-window mechanics all reproduce it identically — flagged for whoever revisits fold3 specifically, not investigated further here (out of this session's scope).
- Logged all 3 rows to `results/sessions/20260823_1525_retro-opp-adjust.csv` and, since all 3 beat the current champion, to `outputs/experiments_v2.csv` per CLAUDE.md's leaderboard rule ((a) session's best + (b) any row beating the champion — here that's all three).
- **Not adopted this session — reported per the task's explicit "STOP after reporting" scope, config restored to the disabled default** (`season_motivation.opponent_adjusted_efficiency_enabled: false`, verified via `load_config()` post-run and `git status` showing `configs/config.yaml`'s working diff only adds the new disabled-by-default fields, no adopted default changed). This is a genuinely promising, unusually cross-window-consistent result — stronger and more robust than several previously-adopted candidates by the project's own bar — but adoption is a decision for a human/a dedicated follow-up session, not this scoping test.
- Next: a human decision on whether to adopt (which window — L5 has the best mean but L10/L20 are close behind and the direction is consistent regardless, so window choice is low-stakes here, unlike the `opponent_adjusted_form_score` precedent where window choice flipped the verdict entirely); if adopted, investigate fold3's reproducible regression specifically before or alongside flipping the flag.

---

**`opponent_adjusted_efficiency` fold3 regression — diagnosis** (2026-08-23, read-only follow-up, no config/code change)
- Question: is fold3's regression (val 2022-10-18→2023-04-09, the 2022-23 season) explained by a known season-specific anomaly, or a genuine, generalizable weakness in the mechanism?
- Checked against known 2022-23-season events first, before any new computation: this was the "tank for Wembanyama" season (unusually pronounced, widely-documented late-season tanking by several teams) and also the season of the Feb 9, 2023 trade deadline's unusually chaotic superstar movement (Kevin Durant Brooklyn→Phoenix, Kyrie Irving Brooklyn→Dallas, Russell Westbrook LAL→Utah/Clippers, all within days of each other) — both real, dateable events that could plausibly disrupt a rolling/opponent-quality-based feature specifically in this season. Nothing else stood out: no lockout-shortened schedule (full 82 games, unlike 2011-12/2020-21), no expansion/relocation, no rule change with plausible bearing on box-score efficiency. Neither event is mentioned anywhere else in this project's docs as a flagged anomaly for this season specifically — `on_off_splits_log.md`/`a7_phase_log.md` both include 2022-23 as one data point among several folds/seasons with no special call-out.
- Empirical check (`L10`, monkeypatched `load_config` in-process for the treatment run only — genuinely read-only, zero writes to `configs/config.yaml`, confirmed via `git status` before/after): retrained fold3 baseline vs. treatment, reproduced the logged val_score exactly (baseline 1.3708, treatment 1.3784, matching the sweep's fold3 numbers), then bucketed per-game `|diff error|` deltas (treatment − baseline, positive = treatment worse) by date.

  | period | n games | mean Δerror |
  |---|---:|---:|
  | pre-deadline (2022-10-18 → 2023-02-08) | 829 | **+0.167** (worse) |
  | deadline → tank stretch (2023-02-09 → 2023-03-18) | 233 | −0.049 (slightly better) |
  | tank stretch (2023-03-19 → 2023-04-09) | 168 | +0.005 (flat) |

  Monthly: Oct +0.090, Nov **+0.220**, Dec +0.117, Jan +0.176, Feb +0.179, Mar −0.030, Apr −0.251 — regression is concentrated **Oct–Jan**, and the feature actually *improves* over baseline in the exact window (Feb 9 deadline onward, and especially the final tank stretch) where both candidate anomalies would predict the opposite.
- **Both "known anomaly" hypotheses are directly falsified by this data, not just unconfirmed.** If either the trade-deadline chaos or the tank-for-Wemby stretch were driving fold3's regression, the error delta would concentrate in Feb–Apr; instead it's worst before either event starts and reverses to a net improvement during and after both. Neither is the explanation.
- **Real, code-grounded, generalizable mechanism found instead:** `compute_team_offense_defense_history` (`season_motivation.py:608`) builds each opponent's "quality" input as a **lifetime cumulative average** (`grp.cumsum()/grp.cumcount()`, no window, no season reset) — structurally different from the already-adopted, already-in-model `opponent_quality_features` family (`_add_opponent_quality_features`, `feature_builder.py:300`), which uses a rolling `off_eff_L{w}`/`def_eff_L{w}` window, same convention as every other opponent/team-quality signal in the codebase. A lifetime cumulative average is the slowest-adapting quality estimator in the entire feature set by construction — the more games a team has played in the loaded history, the less any single recent game (a trade, a roster overhaul) moves it. Combined with the team's own residual being rolled over its last `window` games (which, early in any season, still substantially overlaps the *prior* season's games, per this codebase's standard no-season-reset convention), both inputs to the early-season residual are most likely to be stale exactly in the Oct–Jan stretch — matching the observed pattern.
- Why fold3 specifically, not folds 2/4/5 too: not established with certainty — plausibly the 2022-23 season followed an unusually active 2022 offseason trade/free-agency period for several teams (documented independently, e.g. Rudy Gobert to Minnesota, Donovan Mitchell to Cleveland, Dejounte Murray to Atlanta — all completed before the 2022-23 opener), which would make the specific staleness bias larger for that season's opening months than for the other folds' opening months. This is offered as a plausible contributing factor, not a confirmed cause — the mechanism (cumulative vs. windowed opponent-quality) is verified in code; which teams/seasons it bites hardest is not further audited here.
- **Read: a genuine, generalizable construction weakness, not a season-specific artifact to write off.** The fix this diagnosis motivates is concrete and testable: swap the opponent-quality input from a lifetime cumulative average to a rolling window (matching `opponent_quality_features`' own already-validated convention) and re-run fold3 (cheap — single fold, no need to re-sweep all 3 windows first). This is also a new instance of this project's running "correlation checks aren't sufficient" lesson (cf. `b2_elo_momentum_and_volatility`, `official_pace_poss_new_columns`) — a *third* distinct failure mode from those two: not collinearity with an existing feature, not collinearity between new columns, but a **quality-estimator construction choice** (cumulative vs. windowed) that no correlation check would have surfaced at all.
- **Recommendation: HOLD, not adopt as currently constructed.** Not a rejection either — this is a real, promising signal (per the original entry's own read) with an identified, fixable, code-level cause for its one weak fold. Adopt only after testing the windowed-opponent-quality variant; if that closes fold3's gap without hurting folds 2/4/5, it's a strictly better construction to adopt instead of the current cumulative-average version.
- Next: a follow-up session should implement the windowed-opponent-quality variant (reuse `_add_opponent_quality_features`'s already-computed `opp_off_quality_L{w}`/`opp_def_quality_L{w}` columns directly as the residual's opponent-quality term, instead of `compute_team_offense_defense_history`'s own cumulative computation) and re-run fold3 in isolation first as a cheap falsification test before a full 5-fold re-sweep.

---

**`opponent_adjusted_efficiency` fold3 fix attempt + blast-radius check** (2026-08-23, follow-up to the diagnosis above)
- **Correction to the diagnosis's own "Next" step, made before implementing**: the diagnosis entry above literally named reusing `opp_off_quality_L{w}`/`opp_def_quality_L{w}` as the fix. Re-checked those columns' actual construction (`_add_opponent_quality_features`, `feature_builder.py:300`) before implementing and found this was imprecise — those columns are a **schedule-strength signal** (rolling average of the quality of the last N *distinct* opponents a team has faced), not "this specific opponent's own current form." Substituting them in verbatim would have made the residual `team_pts − (my own recent strength of schedule)`, which compares today's score against unrelated past opponents rather than today's actual opponent — a category error, not a fix. The semantically-correct fix keeps `compute_team_offense_defense_history`'s existing self-merge-by-opponent-identity structure (unchanged) and only swaps *how* each team's own pre-game quality is computed: lifetime cumulative average (`cumsum`/`cumcount`) → `shift(1).rolling(window, min_periods=1).mean()`, the exact same construction `_add_style_features`' `off_eff_L{w}`/`def_eff_L{w}` already uses. Only the *windowing pattern* transfers from the sibling family, not its literal columns.
- Implementation: `compute_team_offense_defense_history(games_df, window)` now takes `window` (reuses the same value already passed to the outer `compute_opponent_adjusted_efficiency_scores` call — one rolling-window parameter, not two independently tunable knobs) and computes `off_eff_before`/`def_eff_before` via the windowed rolling mean instead of `off_eff_cum_before`/`def_eff_cum_before`'s cumulative one; the self-merge-by-`(game_id, opponent_id)` step is otherwise unchanged. Caller updated (`feature_builder.py`'s `_add_season_motivation_features`). Tests: renamed column references in the 3 existing exact-value assertions (window=10 chosen for those synthetic 2-4-game fixtures makes windowed and cumulative numerically identical, so the original hand-verified numbers still hold unchanged); added one new test, `test_window_actually_windows_not_cumulative`, that uses a short window (1) against a 3-prior-game history specifically to prove the computation is genuinely windowed now, not a cumulative average under a new column name. Full suite: **198/198 passing** (192 baseline + this feature's own pre-existing 5 tests + 1 new one).
- **Fold3 retest (read-only w.r.t. `configs/config.yaml` — same in-process `load_config` monkeypatch technique as the diagnosis, verified via `git status` showing zero diff to the config file before/after), L10, baseline vs. treatment:**

  | | val_score | diff_mae | total_mae |
  |---|---:|---:|---:|
  | baseline | 1.3708 | 10.130 | 14.930 |
  | treatment, OLD (cumulative, pre-fix) | 1.3784 | 10.233 | 14.887 |
  | treatment, NEW (windowed, this fix) | 1.3763 | 10.170 | 14.990 |

  Fold3 delta (treatment − baseline): old +0.0076 → new **+0.0055** — real, reproducible (this project's CatBoost training is exactly deterministic, confirmed elsewhere in this doc) improvement, roughly a **28% reduction** in the regression's magnitude. `diff_mae` improved (10.233→10.170) but `total_mae` moved slightly the other way (14.887→14.990), netting to a smaller but still-positive (still worse-than-baseline) val_score delta.
- **Does NOT resolve fold3 cleanly.** The identified mechanism (cumulative vs. windowed opponent-quality) is confirmed to be a real, measurable *contributor* to fold3's regression — not a misdiagnosis — but it isn't the whole story; something else still makes fold3 the one fold this feature doesn't help. Per this session's own instruction ("if fold3 doesn't resolve, report plainly — don't force a full sweep on a fix that didn't work"), **the full 5-fold × 3-window sweep was NOT re-run.**
- Read on adoption status: this doesn't reopen the original diagnosis's finding that the *unfixed* (cumulative) version already nominally cleared this project's own per-fold adoption bar (majority of folds 2–5 improve — see the original entry). What changes here is narrower and still useful: the specific cumulative-average weakness is confirmed real and now fixed at the code level (better-motivated construction going forward regardless of this feature's own fate), and fold3's gap is smaller, not eliminated — a maintainer choosing to adopt this feature has slightly better evidence with the fix in than without it, but not evidence that fold3 is now a clean pass.
- **Recommendation: HOLD.** Not a resolve-and-adopt, not a reject — the code fix is a genuine improvement worth keeping either way (it's simply a more correct construction), but per-fold-guardrail confidence for *this specific feature* is unchanged in kind (fold3 still regresses) even though it improved in degree. A full 5-fold re-sweep (all 3 windows, correlation checks) is the natural next step for whoever decides adoption is worth pursuing further, but is a deliberate non-action this session.
- **Blast-radius check (read-only, part 2 of this session's task): searched the whole `src/` tree for the same no-window/no-season-reset lifetime-cumulative-average pattern (`cumsum()`+`cumcount()` combined, used as a "team quality" estimator).** Found exactly one other instance, in this same file: `compute_team_performance_history`'s `win_pct_before`/`opponent_win_pct` (`season_motivation.py`, groups by `team_id` only — genuinely lifetime, no window, no season reset) — feeds `compute_opponent_adjusted_form_scores` (`opponent_adjusted_form_enabled`, the win/loss precedent, **already not-adopted** for a documented window-instability: passed CV at window=10, inverted sign at windows 5/15, `docs/features/season_motivation_log.md` section 10). **Not fixed this session** (out of scope per the task's own instruction — flag only). Worth naming explicitly: a lifetime-cumulative opponent-quality term that never varies with the outer window, while only the outer rolling window varied across that 5/10/15 sweep, is a *plausible* contributing mechanism for that earlier instability — not confirmed, not investigated further here, but a concrete lead if that signal is ever revisited. `compute_performance_vs_expectation_scores` is unaffected (Elo-residual-based, doesn't use `win_pct_before`). Two other `cumcount`/`cumsum` usages found and checked, both legitimate (not the same bug): `_build_team_game_log`'s standings computation (`season_motivation.py`, correctly season-scoped — standings are inherently cumulative-to-date by definition) and `feature_builder.py`'s `season_progress` (also correctly season-scoped). No hits anywhere outside `season_motivation.py`/`feature_builder.py`.
- Next: (a) a future session could re-run the full 5-fold × 3-window sweep with the fix in, now that it's confirmed non-trivial (real Δ, not a no-op) — not scheduled; (b) `compute_team_performance_history`'s `win_pct_before` is a known, flagged, un-fixed instance of the same original pattern — candidate for its own future fix + re-test of `opponent_adjusted_form_score`'s window-instability, also not scheduled.

---

**`opponent_adjusted_efficiency` fold3 — fresh diagnosis + full re-sweep + final verdict** (2026-08-24, closing follow-up to the two sessions above)
- Task: with the windowed-opponent-quality fix already in place (fold3 L10 regression +0.0076 → +0.0055, ~28% reduction, not eliminated), find what's left causing fold3's residual regression — checked fresh, not assumed to be the same bug class — then run the full 5-fold × 3-window re-sweep and give a final adopt/reject/hold call.
- **Fresh hypothesis 1 — season-boundary crossing: falsified, and backwards.** The fix's `shift(1).rolling(window)` still groups by `team_id` only (no season reset), same convention as the already-adopted `off_eff_L{w}`/`def_eff_L{w}` — but this feature compounds *two* such windows (the team's own rolling window and the self-merged opponent's own rolling window at the time of each past game), which a plain `off_eff_L{w}` never does, so it wasn't ruled out by that precedent alone. Tested directly: a season-reset variant (`groupby(["season_id","team_id"])`, min_periods=1) of `compute_team_offense_defense_history`, fold3-only, L10. Result: season-reset **val_score=1.3862** (Δ+0.0154 vs. baseline) — *worse* than both the unfixed cumulative version (+0.0076) and the current windowed fix (+0.0055). Resetting removes exactly the cross-season history that (per hypothesis) should have been contaminating the estimate; instead removing it hurts, meaning the extra history is net-stabilizing, not net-harmful. Hypothesis rejected, not just unconfirmed.
- **Fresh hypothesis 2 — 2022 offseason "superteam" trade staleness (Gobert→MIN, Mitchell→CLE, Murray→ATL, plus sending teams UTA/SAS): falsified, and backwards.** The original fold3-diagnosis session (previous entry) flagged this as a plausible but untested contributing factor. Bucketed the current-fix-vs-baseline per-game `|diff error|` delta by whether either team in the game was one of these 5 franchises. If stale pre-trade rosters were driving the regression, trade-involved games should show a *larger* delta. Measured: non-trade games mean Δ=+0.059 (n=847) vs. trade-involved games mean Δ=+0.001 (n=383) — trade-involved games are barely affected at all, non-trade games carry essentially all of the regression. Same reversal holds isolating November 2022 (the single worst month, see below): non-trade Δ=+0.207 (n=148) vs. trade-involved Δ=+0.131 (n=74). Hypothesis rejected.
- **Monthly error-delta re-check (same method as the prior diagnosis) shows the pattern has genuinely shifted, not just shrunk.** Pre-fix, the regression concentrated Oct–Jan and reversed to an improvement Feb onward. Post-fix: Oct is now ~flat (+0.014, down from the pre-fix +0.09 to +0.22 range) and Jan is now slightly *negative* (−0.030, i.e. improved) — both cleaned up by the fix, consistent with its intent. But Nov (+0.182, the single worst month), Dec (+0.079), and **Feb (+0.110, now positive — it was the start of the *improving* stretch pre-fix)** are still or newly positive; only Mar (+0.033, near-flat) and Apr (−0.398, strongly improved, low volume) end the season well. No single remaining month or mechanism explains this diffuse pattern — both fresh hypotheses tested above were natural candidates for explaining specifically the Nov concentration and both were falsified. **No further explanation surfaces after this investigation; reported plainly rather than continuing to search.**
- **Full 5-fold × 3-window re-sweep, fix applied** (`scripts/run_opponent_adjusted_efficiency_sweep.py`, session `20260824_1638_opp-adjust-close`), vs. champion (`b2_elo_momentum`, val_score_mean=1.3803, per-fold 1.4336/1.3888/1.3708/1.3479/1.3605):

  | window | mean val (fix) | Δ vs champion | mean val (pre-fix, same window) | fold1 Δ | fold2 Δ | fold3 Δ | fold4 Δ | fold5 Δ | folds improving |
  |---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
  | L5  | 1.3799 | −0.0004 | 1.3788 | −0.0034 | −0.0085 | **+0.0106** | −0.0002 | −0.0009 | 4/5 |
  | L10 | 1.3789 | −0.0014 | 1.3790 | −0.0015 | −0.0096 | **+0.0055** | −0.0013 | −0.0002 | 4/5 |
  | L20 | 1.3808 | **+0.0005** | 1.3797 | +0.0010 | −0.0100 | **+0.0166** | −0.0064 | +0.0010 | **2/5** |

  Pre-CV correlation checks (all 3 windows, both new columns against existing `off_eff_L{w}`/`def_eff_L{w}` and `opp_{off,def}_quality_L{w}`, and against each other) stayed in the same low-to-moderate ranges already reported for the pre-fix version (r=0.55–0.67 vs. own-side `off_eff`/`def_eff`, r≈0.10–0.17 vs. opponent-quality, r=−0.32 to −0.34 between the two new columns themselves) — no new collinearity concern introduced by the fix.
- **The fix does not uniformly help — it only helps at the one window it was diagnosed and validated on (L10), and actively breaks cross-window robustness at the others.** Because `compute_team_offense_defense_history` now threads the *same* `window` value into both the outer residual rolling-mean and the inner opponent-quality estimator (previously only the outer term varied with window; the inner term was a flat lifetime-cumulative average regardless of window), changing the window now changes two things at once, not one. Concretely: L10's fold3 delta improved as designed (+0.0076→+0.0055) and its mean edge over champion held (Δ−0.0014, essentially unchanged from pre-fix's Δ−0.0013). L5's fold3 delta got *worse* than its own pre-fix value (+0.0106 vs. whatever L5 showed pre-fix — pre-fix reporting didn't split by window, but L5's post-fix regression is larger than L10's). **L20 is the clearest casualty: fold3 delta nearly tripled the pre-fix magnitude (+0.0166) and fold1/fold5 flip from small-improve/flat to regress, dropping L20 from 4/5-folds-improving (pre-fix) to 2/5 (post-fix) — L20 now fails this project's own per-fold adoption guardrail outright**, something it did not do before the fix.
- **This directly reproduces the failure mode this candidate was previously praised for avoiding.** The original scoping session (2026-08-23) explicitly contrasted this feature favorably against its closest precedent, `opponent_adjusted_form_score` (window=10 passed CV, inverted sign at windows 5/15, rejected for exactly that instability) — noting "window choice is low-stakes here... unlike the `opponent_adjusted_form_score` precedent where window choice flipped the verdict entirely." That was true of the *original* (cumulative, pre-fix) construction. It is no longer true of the fixed (windowed) construction: L20 now flips from pass to fail on the same per-fold guardrail, and L5's margin has visibly thinned. The correctness fix, by coupling the window parameter to two estimators instead of one, reintroduced the precedent's own documented failure mode into this candidate.
- **Final recommendation: REJECT.** Not held — this closes the thread. Two reasons, either alone sufficient: (1) fold3's regression has now survived three dedicated diagnostic passes across two sessions (cumulative→windowed fix: real but partial; season-boundary reset: falsified, backwards; trade-staleness: falsified, backwards) without a clean resolution, and the remaining pattern is diffuse across several months with no single further mechanism identified — continuing to chase it is exactly the "shrinking hit rate for growing diagnostic cost" pattern this phase's own rollup already named as the reason to stop broad representation-search on this feature set. (2) The fix needed to make progress on fold3 measurably destroys the feature's original best-in-phase property (cross-window robustness) at L20 and thins it at L5, leaving only a single window (L10) that both fixes fold3 partially and clears the guardrail — the same single-window-only shape this project already rejected once for the closest related feature. `season_motivation.opponent_adjusted_efficiency_enabled` stays at its committed default (`false`) — config confirmed unchanged (`git status` shows no diff to `configs/config.yaml` from this session's diagnostic runs, which used the same in-process `load_config`-monkeypatch technique as prior sessions; the full re-sweep's own `configs/config.yaml` mutation was restored by its own `finally` block, also confirmed via `git status`). The windowed-opponent-quality construction in `compute_team_offense_defense_history` is kept as-is in the code (still a more correct construction than the original lifetime-cumulative version it replaced, per the prior session's reasoning) even though the feature itself is rejected — nothing about this session's finding makes the cumulative version preferable, it only shows the windowed version's own robustness is narrower than hoped. Logged both rows that beat the champion (L5, L10 — L20 does not) to `outputs/experiments_v2.csv` per CLAUDE.md's leaderboard rule, run_names suffixed `_windowed_fix` to distinguish from the already-logged pre-fix rows from session `20260823_1525_retro-opp-adjust`; all 3 rows also logged to `results/sessions/20260824_1638_opp-adjust-close.csv`. `compute_team_performance_history`'s `win_pct_before` (the still-unfixed instance of the original cumulative-average pattern, feeding the already-rejected `opponent_adjusted_form_score`) remains flagged, not fixed — out of scope, and now lower-priority given this session's finding that windowing that particular estimator is not a reliable win either.

---

**`b2b_rest_diff`** (2026-08-24) — `docs/EXPLORATION.md`'s decisive-shortlist item 1, `b2b_diff`/`rest_diff` home-minus-away differentials.
- Confirmed genuinely unimplemented before starting (grepped `feature_builder.py`, `configs/config.yaml`, this doc — no hits) and not a duplicate of `second_of_b2b`/`back_to_back` (those are per-team scalars; this is the cross-team differential, never built).
- Implementation: 2 new columns added directly in `_add_rest_features` (`feature_builder.py`), computed from that function's own already-merged `home_team_back_to_back`/`away_team_back_to_back`/`home_team_rest_days`/`away_team_rest_days` arrays — `b2b_diff = home_back_to_back − away_back_to_back`, `rest_diff = clip(home_rest_days − away_rest_days, −5, 5)`. No new merge, no leakage surface beyond what `_add_rest_features` already has (both inputs are already point-in-time, `shift`-based via the long-frame diff). Always-on, no config flag — matches `_add_matchup_features`'s existing precedent for hand-built differentials (also unconditional). 148 → 150 features. Full suite unaffected: `test_rest_features_venue_blind.py`/`test_overall_form_venue_blind.py` (4 tests) still pass unchanged.
- **Pre-CV correlation check (required by this session's own instruction, reported regardless of value):** against the existing per-team rest columns (n=9,509 non-null rows, full history): `b2b_diff` vs. `home_team_back_to_back` = 0.626, vs. `away_team_back_to_back` = −0.698, vs. `home/away_rest_days` ≈ 0 (−0.008/+0.027); `rest_diff` vs. `home_team_rest_days` = +0.047, vs. `away_team_rest_days` = −0.077, vs. `home/away_back_to_back` = −0.43/+0.46. These moderate correlations are the expected/structural kind (the differential is arithmetically built from exactly these two columns, same relationship `elo_diff` has to `home_team_elo`/`away_team_elo`) — not evidence of a problem. `b2b_diff` vs. `rest_diff` (the two new columns against **each other**, per the `official_pace_poss_new_columns` lesson to check this explicitly) = −0.668 — moderate, not a near-duplicate pair (that failure mode showed up at r=0.976). Against `POINT_DIFF`: `b2b_diff` = −0.077, `rest_diff` = +0.053 — both replicate `docs/EXPLORATION.md`'s measured values (−0.079/+0.050) almost exactly.
- **Full 5-fold CV** (`b2b_rest_diff`, 150 cols) vs. baseline (`b2_elo_momentum`, 148 cols, val_score_mean 1.3803, per-fold 1.4336/1.3888/1.3708/1.3479/1.3605): mean val_score **1.3786**, Δ**−0.0017** (improvement). Per-fold delta (new − baseline): fold1 **−0.0049**, fold2 **−0.0115**, fold3 +0.0013, fold4 +0.0053, fold5 +0.0012 — **2 of 5 folds improve, 3 regress**, failing this project's established per-fold-majority guardrail (same guardrail `a4_vif_trim_overall_form` failed on, at a smaller mean-Δ) despite the larger, more favorable mean movement. The two improving folds (fold1/fold2) move by roughly 4-9x more than any of the three regressing folds (all ≤0.0053) — the mean improvement is being carried entirely by 2 folds, not broad-based.
- **`market_benchmark.py`** (`tag=b2b_rest_diff`, fold5) vs. `b2_elo_momentum` baseline: diff_mae 11.421 (baseline 11.430, **better**), total_mae 15.457 (baseline 15.357, **worse**, +0.100), win_acc 0.6787 (baseline 0.6795, worse, −0.0008), brier 0.20796 (baseline 0.20779, worse, +0.00017) — **3 of 4 metrics worse**, 1 better. Leans negative, same direction as the CV fold-majority result.
- **Recommendation: REJECT.** Two consistent, independent signals point the same way: the CV result fails the fold-majority guardrail this project has applied to every prior candidate (2/5 improve is the same count that sank `a4_vif_trim_overall_form`), and `market_benchmark` moves 3/4 metrics the wrong way. The favorable-looking mean val_score is a real number, not a bug, but it's concentrated in 2 folds rather than broad-based — the same "large minority improvement outweighs a small-majority regression in the mean, fails the fold-count check anyway" shape as `a4`, not the "broad 3-4/5 improvement, small regressions only" shape that got `b2_elo_momentum` adopted. Code reverted (`_add_rest_features` back to its pre-session state, confirmed via `git diff` showing zero changes) — same treatment as `a4_vif_trim_overall_form`/`b1_style_and_rolling_decay_weighted`, both also unconditional (no config flag) rejected changes. Unlike `official_pace_enabled`, there was no existing enable/disable convention on `_add_rest_features` to gate this behind, so "kept but disabled" wasn't an available middle option here.
- Row logged to `outputs/experiments_v2.csv` (auto-logged by `train_model.py`, run `b2b_rest_diff`) per CLAUDE.md's "every run appends a row" rule — this is a manual one-off experiment, not a research session, so no separate `results/sessions/` file.

---

**`trend_slope_off_def_L10`** (2026-08-24) — `docs/NEXT_PHASE_SESSIONS.md`'s creative-feature-engineering backlog item 1, explicit within-window linear-regression slope of off_eff/def_eff.
- Confirmed genuinely untested before starting, and distinct from the already-rejected `diff_avg_L5 − diff_avg_L20` two-point proxy (`docs/EXPLORATION.md` Area 1, exact residual correlation 0.0000 against the L5/L10/L20 baseline) — that result is about a specific difference-of-two-existing-columns statistic, not a true OLS slope that weights every game in the window. Implementation: new `_add_trend_features` method in `feature_builder.py`, gated by a new `trend_features.enabled`/`trend_features.window` config section (ships `false`/`10`, same convention as `EloFeaturesConfig`/`InjuryFeaturesConfig`). Computes `home_team_off_eff_slope_L10`/`home_team_def_eff_slope_L10` (and away-side equivalents) via `shift(1).rolling(window, min_periods=2).apply(_ols_slope, raw=True)` directly on `PTS_home`/`PTS_away` (not on the already-averaged `off_eff_L{w}` columns), so the slope reflects the raw per-game sequence within the window, not a slope-of-means. Single window tested (L10), per this session's own scope — L5/L20 not swept. Added alongside the existing `off_eff_L{w}`/`def_eff_L{w}` mean columns, not replacing them (148 → 152 features when enabled, 4 new columns: `home_team`/`away_team` × `off_eff_slope_L10`/`def_eff_slope_L10`). 9 new regression tests (`tests/test_trend_slope_features.py`): exact-value check against a hand-computed OLS fit, no-leakage (fewer than 2 prior games → NaN), boundary (exactly 2 prior games → first real value), only-uses-strictly-prior-games (perturbing a later game's points doesn't change an earlier row's slope), and a check that the statistic genuinely isn't endpoint-only (unlike a `diff_avg`-style two-point proxy, a middle point changes the value). Full suite: 207/207 passing at the shipped default (`false`) — 4 pre-existing test files' `MagicMock()`-based config fixtures (`test_h2h.py`, `test_overall_form_venue_blind.py`, `test_rest_features_venue_blind.py`, `test_rolling_shooting_pct.py`) needed `trend_features = MagicMock(enabled=False)` added explicitly, the same mechanical fallout of adding a new boolean config field already seen with `official_pace_enabled` (a bare `MagicMock()` attribute is truthy by default) — unrelated to the ablation result itself.
- **Pre-CV correlation check** (fold5 train features, n=7,059, required by this session's own instruction, reported regardless of value): `off_eff_slope_L10`/`def_eff_slope_L10` vs. existing `off_eff_L{w}`/`def_eff_L{w}` — near-zero at L10 (−0.005 to −0.008, the window the slope shares) and L20 (−0.035 to −0.059), moderate at L5 (0.48–0.50, expected since L5's own window overlaps the most recent games the slope is most sensitive to). Vs. the already-rejected `diff_avg_L5 − diff_avg_L20` proxy (reconstructed post-hoc from its own component columns, since the proxy itself was never materialized): moderate, 0.39–0.41 (off) / −0.41 (def) — a real relationship (both statistics respond to the same underlying recent-form shift) but well below the ~0.976–0.998 near-duplicate threshold that flagged `b1_style_and_rolling_decay_weighted`/`official_pace_poss_new_columns`. No collinearity red flag from either check.
- **Full 5-fold CV** (152 cols) vs. champion (`b2_elo_momentum`, 148 cols, val_score_mean 1.3803, per-fold 1.4336/1.3888/1.3708/1.3479/1.3605): mean val_score **1.3809**, Δ**+0.0006** (marginally worse — val_score is minimized). Per-fold delta (new − champion): fold1 **+0.0044**, fold2 −0.0038, fold3 **+0.0057**, fold4 −0.0010, fold5 −0.0025 — **3 of 5 folds improve (2,4,5), 2 regress (1,3)** — a bare fold-count majority, but the two regressing folds are larger in magnitude (+0.0044, +0.0057) than any of the three improving folds (−0.0038, −0.0010, −0.0025), which is why the mean ends up slightly unfavorable despite the 3/5 count. Not a concentrated-in-one-fold pattern like `b2b_rest_diff`'s fold1/fold2 — the regression here is spread across fold1 and fold3, not one outlier.
- **`market_benchmark.py`** (`tag=trend_slope_off_def_L10`, fold5) vs. `b2_elo_momentum` baseline: diff_mae 11.470 (baseline 11.430, worse, +0.040), total_mae 15.395 (baseline 15.357, worse, +0.038), win_acc 0.6730 (baseline 0.6795, worse, −0.0066), brier 0.2089 (baseline 0.2078, worse, +0.0011) — **all 4 metrics worse**, unanimous.
- **Recommendation: REJECT.** The CV mean is worse (not just "not better"), and the fold pattern that would normally offset a small unfavorable mean (a broad, small-regression fold-majority, the `b2_elo_momentum` shape) isn't present here — the 3/5-improve count is real but thin, outweighed in magnitude by 2 larger regressions, so it doesn't rescue an already-unfavorable mean the way it would need to. `market_benchmark` is unanimous against it, the clearest signal of the three. This is a genuine null-to-slightly-negative result, not a borderline case: no metric anywhere in this session's evidence favors adoption. `trend_features.enabled` stays `false` (config confirmed via `git status`/`git diff` before/after — the file was set to `true` only for the duration of the correlation check and CV run, reverted immediately after). Code (the `_add_trend_features` method, `_ols_slope` helper, and the `trend_features` config section) is kept, not deleted — same treatment as `official_pace_enabled`/`opponent_adjusted_efficiency_enabled`, both other rejected-but-kept-as-disabled-options — in case a future session wants to test a different window (L5/L20 untested) or a different underlying statistic on the same infrastructure.
- Row logged to `outputs/experiments_v2.csv` (auto-logged by `train_model.py`, run `trend_slope_off_def_L10`) — manual one-off experiment, no separate `results/sessions/` file.

---

**`clutch_split_off_def_L10_L20`** (2026-08-24) — `docs/NEXT_PHASE_SESSIONS.md`'s creative-feature-engineering backlog item 2, clutch/close-game performance split (the confirmed defensible half of item 2; skewness was already dropped from consideration in the prior scoping pass).
- Implementation: new `_add_clutch_split_features` method in `feature_builder.py`, gated by a new `clutch_split_features.enabled`/`.margin_threshold`/`.windows` config section (ships `false`/`5`/`None`, same convention as `TrendFeaturesConfig`). Computes `close_off_eff_L{w}`/`close_def_eff_L{w}` (home/away sides) via a venue-blind long-frame (same team-perspective pattern as `_add_rolling_features`'s overall-form addition): each team's own points/opponent points are masked to `NaN` on any past game that wasn't close (`abs(POINT_DIFF) > margin_threshold`), then `shift(1).rolling(w, min_periods=1).mean()` — pandas' own NaN-skipping means the average is taken only over whichever close games happened to fall in the window, consistent with every other `*_L{w}` feature's window semantics rather than a bespoke "last w close games" lookback. Point-in-time: the close/blowout split key comes from an already-completed past game (`shift(1)` applied before conditioning), not the game being predicted. Added alongside the existing unconditional `off_eff_L{w}`/`def_eff_L{w}`, not replacing them. 6 new regression tests (`tests/test_clutch_split_features.py`): exact masked-average value on a hand-built sequence, zero-close-games-in-window → NaN, `margin_threshold` is genuinely configurable (a margin of exactly 6 flips from excluded to included), only-uses-strictly-prior-games (leakage check), and a venue-blind test confirming a team's road game contributes to a later home game's feature. Full suite: 213/213 passing at the shipped default (`false`) — same 4 pre-existing `MagicMock()` config-fixture files needed `clutch_split_features = MagicMock(enabled=False)` added, same mechanical fallout already seen twice this phase (`official_pace_enabled`, `trend_features`).
- **Pre-CV data-hunger check (required by this session's own instruction, reported before any CV run since a near-empty window undermines the feature regardless of CV outcome)**: league-wide, 26.1% of team-games are close (margin≤5). Per-window count of close games actually falling within a team's last w games (fold5 train features, n≈7,059-14,118 team-game rows): **L5 — mean 1.30, median 1.0, 22.4% of rows have ZERO close games in the window, 38.7% have exactly one (61.1% combined are either undefined or a single-game "average", not a real one)**; L10 — mean 2.59, median 2.0, 5.2% zero, 17.6% exactly one (50.0% combined <3 games); L20 — mean 5.12, median 5.0, 0.8% zero, 2.5% exactly one (9.7% combined <3 games). This directly confirms the data-hunger risk flagged at scoping, and confirms it's concentrated at the shortest window, not uniform across all three. **L5 was excluded from the tested feature set on this evidence alone, before any CV run** — a partial pre-CV rejection, one of this session's explicitly permitted valid outcomes. L10/L20 were judged sample-adequate enough to proceed to CV (L10 is still thin at the median, flagged as the weaker of the two tested windows going in).
- **Pre-CV correlation check** (same fold5 sample): `close_off_eff_L{w}`/`close_def_eff_L{w}` vs. existing `off_eff_L{w'}`/`def_eff_L{w'}` (all 3×3 window pairs, both sides) — moderate throughout, r=0.29–0.55, no pair anywhere near the ~0.98 near-duplicate threshold. Matches the MODERATE collinearity risk predicted at scoping (built from the same underlying per-game data, conditioned on a subset — related but not redundant) — no new red flag, and no surprise either.
- **Full 5-fold CV** (L10+L20 only, 156 cols, via `clutch_split_features.windows: [10, 20]`) vs. champion (`b2_elo_momentum`, 148 cols, val_score_mean 1.3803, per-fold 1.4336/1.3888/1.3708/1.3479/1.3605): mean val_score **1.3824**, Δ**+0.0021** (worse). Per-fold delta (new − champion): fold1 **+0.0036**, fold2 **+0.0025**, fold3 **+0.0072**, fold4 **+0.0012**, fold5 −0.0039 — **4 of 5 folds regress, only fold5 improves** — a broad-based regression, not concentrated in one or two folds the way some earlier borderline candidates were.
- **`market_benchmark.py`** (`tag=clutch_split_off_def_L10_L20`, fold5) vs. `b2_elo_momentum` baseline: diff_mae 11.420 (baseline 11.430, marginally better, −0.010), total_mae 15.426 (baseline 15.357, worse, +0.069), win_acc 0.6763 (baseline 0.6795, worse, −0.0033), brier 0.2073 (baseline 0.2078, marginally better, −0.0005) — mixed, 2 of 4 worse (one meaningfully, total_mae), 2 marginally better, leaning negative overall and consistent with the broad CV regression.
- **Recommendation: REJECT.** Three independent lines of evidence, all pointing the same direction or providing no offsetting positive: the data-hunger check confirms L5 (the shortest, cheapest-to-estimate window in every other family) is genuinely too sparse to trust and was correctly excluded before spending a CV run on it; even with that exclusion, the CV result on the two better-sampled windows is a clear, broad regression (4/5 folds, not a thin majority like `trend_slope_off_def_L10`'s); and `market_benchmark` is mixed-to-negative, not offsetting. Unlike `b2b_rest_diff` (favorable mean, failed on fold-count) or `trend_slope_off_def_L10` (near-flat mean, ambiguous fold pattern), this is an unambiguous reject on the primary aggregated-validation-mean criterion itself — no secondary guardrail argument is even needed. `clutch_split_features.enabled` stays `false` (config confirmed via `git status`/`git diff` before/after — flipped `true`, with `windows: [10, 20]`, only for the duration of the diagnostic check and CV run, reverted immediately after). Code kept, not deleted — same treatment as every other rejected-but-kept-disabled option this phase (`official_pace_enabled`, `opponent_adjusted_efficiency_enabled`, `trend_features`) — the new `windows` config field also makes L5 (or any other window) directly testable later without new code, should league-wide close-game rates ever justify revisiting it.
- Row logged to `outputs/experiments_v2.csv` (auto-logged by `train_model.py`, run `clutch_split_off_def_L10_L20`) — manual one-off experiment, no separate `results/sessions/` file.
